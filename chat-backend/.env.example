# Database connection string (PostgreSQL)
# Render will auto-populate this if you add PostgreSQL service
DATABASE_URL=postgresql://username:password@host:5432/database

# Server port
PORT=3000

# Node environment
NODE_ENV=production
