# Node modules
node_modules/

# Build output
dist/
build/
*.min.js
!**/js/lib/*.min.js
!web2/shared/*.min.js
*.min.css
*.min.html

# Logs
*.log
**/logs/

# Playwright MCP output (screenshots/snapshots khi browser-test)
.playwright-mcp/
label-lab-*.png
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment files
.env
.env.local
.env.production

# Firebase Service Account Key (NEVER commit)
serviceAccountKey.json
**/serviceAccountKey.json

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Temporary files
tmp/
temp/
*.tmp

# Cache
.cache/
.eslintcache
.parcel-cache/

# Package manager
yarn.lock
pnpm-lock.yaml
dynamic-headers.json

# Secrets file (NEVER commit)
serect_dont_push.txt
serect_dont_push*.txt

# MCP config (contains API keys)
.mcp.json

# Windows reserved filenames
nul

# User scratch / debug dumps at repo root (not source code)
/dung.txt
/sai.txt
/text.txt
/scratch-*.txt
/debug-*.txt

# Soquy Firestore backups (chứa dữ liệu cá nhân khách hàng)
render.com/backups/soquy/*.json

# OnCallCX — private recordings + intermediate debug dumps
downloads/oncallcx-recordings/
docs/oncallcx-*.html
docs/oncallcx-*.json
docs/oncallcx-*.sh
docs/oncallcx-page-*.html
docs/oncallcx/*.html
docs/oncallcx/*.json
!docs/oncallcx/portal-map.json

# Resident.vn crawl artifacts — contains auth tokens (localStorage) + private data
downloads/resident-crawl/
downloads/resident-clone-smoke/
downloads/resident-deep-compare/

# n2store jitter monitor — contains cookies + storage state
downloads/n2store-jitter/

# Resident clone mock data — contains customer PII from owner's account
resident/data/*.json
!resident/data/README.md

# Scheduled tasks lock
.claude/scheduled_tasks.lock
downloads/tikreel-ui-study/

# yt-dlp binary downloaded at runtime (Linux on Render, not portable)
render.com/bin/

# Local DB backups (chứa dữ liệu khách hàng - tuyệt đối không commit)
backups/
*.dump
*.sql.gz

# Local Chrome profiles + browser session cache (committed by accident 2026-05-22)
.local/
downloads/n2store-session/console-*.json
downloads/n2store-session/smoke-report-*.json
.extension-last-published-version

# Playwright MCP storageState (chứa JWT loginindex_auth/web2_auth) — KHÔNG commit
downloads/n2store-session/auth-state*.json
.chrome-n2store-debug/

# Barcode comparison test (TPOS vs JsBarcode) — local only
downloads/n2store-session/barcode-compare*.html
downloads/n2store-session/barcode-compare-*.png

# TPOS test captures — chứa auth tokens, KHÔNG commit
downloads/n2store-session/tpos-print-*.html
downloads/n2store-session/tpos-print-*.json
downloads/n2store-session/tpos-print-*.png
downloads/n2store-session/tpos-session-*.json
downloads/n2store-session/tpos-storage.json
downloads/n2store-session/tpos-newpage-*.html
downloads/n2store-session/tpos-download-*

# Refund excel mẫu cho dev đối soát KPI — chứa PII khách thật, KHÔNG commit
docs/orders-report/*.xlsx
pancake-creds.local.txt
downloads/n2store-session/pancake-login-capture.log

# test assets (faces/videos cho beauty test — không commit)
downloads/test/

# codegraph local index (dev tooling, not committed)
.codegraph/
real-*.png
code-*.png
detect-bar*.png
msg-addr-btn*.png
