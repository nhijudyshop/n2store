/* =====================================================
   CHAT PRODUCTS UI - Stub (rebuilt)
   Product panel in chat modal - placeholder
   ===================================================== */

console.log('[ChatProducts-UI] Loaded (stub).');

// Stubs for functions referenced by other modules (conditional assignments)
// These are safe to be undefined - callers check with optional chaining or conditionals

window.removeChatProduct = window.removeChatProduct || function(productId) {
    console.log('[ChatProducts] removeChatProduct stub called:', productId);
};

window.updateChatProductQuantity = window.updateChatProductQuantity || function(productId, qty) {
    console.log('[ChatProducts] updateChatProductQuantity stub called:', productId, qty);
};

window.saveChatProductsToFirebase = window.saveChatProductsToFirebase || function() {
    console.log('[ChatProducts] saveChatProductsToFirebase stub called');
};
