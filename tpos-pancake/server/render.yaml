# Render.com deployment configuration
# Service: n2store-tpos-pancake (Pancake WebSocket Client)

services:
  - type: web
    name: n2store-tpos-pancake
    env: node
    rootDir: tpos-pancake/server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: MAX_EVENTS
        value: 1000
