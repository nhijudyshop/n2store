# gemini-tryon sidecar — gọi Gemini web app (free) bằng cookie + phục vụ HTTP.
# [browser] kéo theo browser-cookie3 để tự đọc cookie từ Chrome đang đăng nhập gemini.google.com.
gemini_webapi[browser]>=1.5.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
