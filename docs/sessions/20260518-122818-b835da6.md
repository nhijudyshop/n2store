<!-- #Note: Đọc CLAUDE.md, MEMORY.md, docs/dev-log.md trước khi code. Cập nhật dev-log sau thay đổi. | Read these files before coding, update dev-log after changes. -->

# Session Resume — 2026-05-18 12:28:18

**Token**: `RESUME:20260518-122818-b835da6`
**Branch**: `main`
**HEAD**: `b835da61c42c243f19842c4d1a9f4834cb70ad39`
**Range**: `bd553a69..b835da6` (1 commit)
**Author**: zxGreenxz
**Generated**: 2026-05-18T12:28:18+07:00

---

## 1. Summary

Fix lỗi lâu lâu miss auto-tag XL "ĐÃ RA ĐƠN" sau khi tạo phiếu bán hàng lẻ (single) hoặc bán hàng hàng loạt (bulk).

User báo đơn ra thành công nhưng không tự động được đánh tag → kẹt ở "CHỜ ĐI ĐƠN". Constraint cứng: **không đụng flow tạo đơn / modal**.

Đã apply 3 layer fix isolated trong function tag — happy path identical, chỉ thay đổi behavior khi fail / race:
- **Fix A**: đợi `ProcessingTagState._isLoaded` thay vì skip im lặng (race F5 → tạo đơn ngay)
- **Fix B**: `saveProcessingTagToAPI` retry exponential backoff 3 lần (network thoáng qua)
- **Fix C**: auto reconcile sau 3s mỗi lần `storeFromApiResult` (defensive layer)

## 2. Files Modified

- `orders-report/js/tab1/tab1-processing-tags.js` — thêm `_waitForPtagLoaded()` helper + queue `_ptagBillCreatedRetryQueue`; sửa `onPtagBillCreated` đợi loaded thay vì skip; sửa `saveProcessingTagToAPI` retry 3 lần với backoff 500/1500/4500ms
- `orders-report/js/tab1/tab1-fast-sale-invoice-status.js` — thêm `setTimeout 3s` cuối `storeFromApiResult` gọi `window.reconcileTagsWithInvoices()` fire-and-forget
- `docs/dev-log.md` — entry mới phía trên

## 3. Key Decisions

- **LOẠI bỏ "Fix 1" (await trong loop)** mặc dù đó là root cause cấp 1 fire-and-forget.
  - Lý do: sẽ phải sửa `forEach → for...of await` + caller (`tab1-sale.js:1314`, `tab1-fast-sale-invoice-status.js:4092`) cũng phải await → delay modal close. User yêu cầu cứng "không tác động modal".
  - Alternative: dùng Fix A + B + C combo để gián tiếp đảm bảo tag eventually consistent — không cần đụng caller.

- **Helper `_waitForPtagLoaded` poll 100ms, timeout 10s** thay vì subscribe event.
  - Lý do: `ProcessingTagState` không expose event/Promise loaded hiện tại. Poll 100ms × 100 iterations là rẻ. 10s đủ cho slow network init.
  - Alternative đã reject: refactor ProcessingTagState để expose Promise — out of scope (đụng nhiều nơi).

- **`setTimeout 3s` (Fix C) chứ không phải 1s/5s.**
  - Lý do: 3s đủ để `saveProcessingTagToAPI` retry lần 1 (500ms) + 2 (1500ms) xong = ~2s. Reconcile chạy sau là layer cuối cùng.
  - Idempotency: `reconcileTagsWithInvoices` skip nếu `category !== CHO_DI_DON` → an toàn re-run.

- **Queue `_ptagBillCreatedRetryQueue` chỉ trong-memory (Set), không persist.**
  - Lý do: nếu page F5, reconcile lúc load sẽ catch lại từ invoice state (đã có sẵn logic ở line 1641). Không cần persist queue.

## 4. Open Todos / Next Steps

- [ ] **User test thực tế** với bulk 5-10 đơn để confirm zero impact lên modal + 100% đơn được tag.
- [ ] **Smoke test** với persistent browser session (CLAUDE.md "LIVE CODING workflow") — verify network tab thấy retry khi throttle Offline.
- [ ] (Optional) Theo dõi prod 1-2 ngày: nếu vẫn còn miss tag → cân nhắc Fix 1 (await trong loop) với UI feedback "đang đánh tag... X/N".
- [ ] (Optional) Expose `_ptagBillCreatedRetryQueue` ra `window.__debugPtagRetryQueue` để dev tools inspect khi cần debug.

## 5. Context Pointers

- `C:\Users\Nguyen Tam\.claude\plans\ki-m-tra-k-l-i-stateless-dahl.md` — plan đầy đủ (root cause + fix strategy + verification)
- `orders-report/js/tab1/tab1-processing-tags.js:696-729` — Fix B (`saveProcessingTagToAPI` retry)
- `orders-report/js/tab1/tab1-processing-tags.js:1452-1515` — Fix A (`_waitForPtagLoaded` + `onPtagBillCreated`)
- `orders-report/js/tab1/tab1-processing-tags.js:1641-1687` — `reconcileTagsWithInvoices` (đã expose `window.reconcileTagsWithInvoices` ở line 6430)
- `orders-report/js/tab1/tab1-fast-sale-invoice-status.js:925-946` — Fix C (post-create reconcile)
- `orders-report/js/tab1/tab1-fast-sale-invoice-status.js:732-928` — `storeFromApiResult` (KHÔNG sửa loop)
- `docs/dev-log.md` (entry 2026-05-18) — chi tiết changes

## 6. Notes for Next Session

- **Script `save-session-resume.sh` cần Python3** — Windows alias trỏ Microsoft Store stub → script fail. Phải tạo session resume thủ công (như file này). Workaround: cài Python thật, hoặc patch script dùng `node` thay `python3`.
- Stop hook `.claude/scripts/hooks/stop-auto-commit-push.sh` có thể conflict nếu chạy lại (đã commit + push manual rồi).
- Nếu cần debug bulk tag: F5 → mở DevTools Console → tạo bulk → grep log `[PTAG] saveProcessingTagToAPI retry` / `_waitForPtagLoaded` / `post-create reconcile`.
- ProcessingTagState là IIFE module ~6990 dòng, đã có window exports: `ProcessingTagState`, `reconcileTagsWithInvoices`, `onPtagBillCreated`, `saveProcessingTagToAPI` (verify nếu cần).

## 7. Previous Session (Chain Pointer)

- Previous: [20260518-121431-c6e4d31.md](20260518-121431-c6e4d31.md)

**Chain walking** (Claude tự làm khi nhận token):

1. Đọc file hiện tại.
2. Nếu Previous ≠ "INITIAL" → đọc thêm file previous.
3. Lặp lại tối đa **N=3 levels** mặc định (tránh context bloat).
4. Nếu user nói "đọc full chain" → đi hết tới INITIAL.

---

## How to resume

User paste vào chat mới:

```
RESUME:20260518-122818-b835da6
```

Claude sẽ:

1. `Read` file này.
2. Walk chain theo section 7 (mặc định 3 levels, hoặc theo yêu cầu user).
3. Tóm tắt 2-3 câu để xác nhận hiểu đúng.
4. Tiếp tục từ phần "Next Steps" của session gần nhất.
