<!-- #Note: Đọc CLAUDE.md, MEMORY.md, docs/dev-log.md trước khi code. Cập nhật dev-log sau thay đổi. | Read these files before coding, update dev-log after changes. -->

# Session Resume — 2026-06-11 17:47:22 +07

**Token**: `RESUME:20260611-174722-943e783`
**Branch**: `main`
**HEAD**: `943e7838d2967125effcb04949a697f8d4a4a2a3`
**Range**: `f50f1b9..943e783` (2 commit(s))
**Author**: zxGreenxz <nguyentamca165@gmail.com>
**Generated**: 2026-06-11T10:47:22Z

---

## 1. Summary

Verify prod handover-batch PASS + fix handover_at gio VN + cache-bust v= 2 trang; cleanup ticket TEST xong

Hoàn tất feature liên thông CSKH → delivery-report (token trước `RESUME:20260611-173556-0686f08`): verify prod toàn bộ flow handover-batch bằng ticket TEST (SĐT `0123456788`, order `TEST-HANDOVER-*`) — claim/idempotent/action_history/normalize phone đều PASS. Phát hiện + fix `handover_at` lưu UTC naive lệch −7h (pg parser append +07:00) → `NOW() AT TIME ZONE 'Asia/Ho_Chi_Minh'`, verify lại khớp giờ thật. Bump cache-bust `?v=` cho 3 JS. Cleanup hard-delete 3 ticket TEST.

## 2. Files Modified

- `delivery-report/index.html` — bump `delivery-report.js?v=20260611a` (cache-bust feature mới)
- `issue-tracking/index.html` — bump `script.js?v=20260611b` + `api-service.js?v=20260611a`
- `render.com/routes/v2/tickets.js` — (commit f50f1b9) fix `handover_at = NOW() AT TIME ZONE 'Asia/Ho_Chi_Minh'`
- `docs/dev-log.md` — (commit f50f1b9) ghi kết quả verify + cảnh báo bug có sẵn created_at lệch −7h
- `docs/sessions/*` — chain + latest snapshots

## 3. Key Decisions

- `handover_at` lưu **giờ VN naive** (đúng convention pg parser OID 1114 append `+07:00` — xem `render.com/db/pool.js:11`), KHÁC với `created_at`/`updated_at` của bảng (DEFAULT CURRENT_TIMESTAMP = UTC naive, hiển thị lệch −7h từ trước — bug có sẵn NGOÀI scope, chưa fix vì đụng data cũ + nhiều chỗ hiển thị).
- KHÔNG click export thật trên prod để test — export thật sẽ claim ticket THẬT (đúng behavior feature). Verify qua API trực tiếp với ticket TEST là đủ coverage data path.

## 4. Open Todos / Next Steps

- [ ] User export thật lần đầu ở tab Thu về → quan sát 2 cột + badge bên CSKH (SSE tự refresh).
- [ ] (Tùy chọn) Fix bug có sẵn: `created_at` các ticket hiển thị lệch −7h trên trang CSKH (cần user duyệt vì ảnh hưởng data/UI rộng).
- [ ] (Tùy chọn) Nút un-mark bàn giao trên CSKH nếu kế toán xuất nhầm.
- [ ] (Tùy chọn) Port `scripts/save-session-resume.sh` 2 block python3 → node (máy Windows không có python3; hiện dùng node replica tạm ở Temp).

## 5. Context Pointers

- `docs/dev-log.md` — entry "[delivery-report][issue-tracking][render] Liên thông CSKH → Xuất excel Thu về"
- `render.com/routes/v2/tickets.js` — route `POST /handover-batch` + `ensureHandoverSchema`
- `render.com/migrations/076_ticket_handover_to_shipper.sql`
- `render.com/db/pool.js:11` — pg parser timestamp +07:00 (gốc vụ lệch giờ)
- Session trước (full context feature): `docs/sessions/20260611-173556-0686f08.md`

## 6. Notes for Next Session

- Endpoint prod đã LIVE + verified; DB sạch (3 ticket TEST đã hard-delete, unique index slot TEST-HANDOVER-* đã giải phóng).
- Máy Windows: không python3/psql/secret file — session resume dùng node replica; test DB qua prod API với data TEST-prefix.
- Session Mac song song push liên tục → luôn `git pull --rebase` trước push.

## 7. Previous Session (Chain Pointer)

> Một conversation có thể có nhiều token tạo dần qua các turn. Section này link đến session trước để Claude tự lần ngược, ghép đủ ngữ cảnh khi user chỉ paste **token cuối**.

- Previous: [`RESUME:20260611-174149-f50f1b9`](./20260611-174149-f50f1b9.md)

**Chain walking** (Claude tự làm khi nhận token):

1. Đọc file hiện tại.
2. Nếu Previous ≠ "INITIAL" → đọc thêm file previous.
3. Lặp lại tối đa **N=3 levels** mặc định (tránh context bloat).
4. Nếu user nói "đọc full chain" → đi hết tới INITIAL.

---

## How to resume

User paste vào chat mới:

```
RESUME:20260611-174722-943e783
```

Claude sẽ:

1. `Read` file này.
2. Walk chain theo section 7 (mặc định 3 levels, hoặc theo yêu cầu user).
3. Tóm tắt 2-3 câu để xác nhận hiểu đúng.
4. Tiếp tục từ phần "Next Steps" của session gần nhất.
