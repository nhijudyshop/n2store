<!-- #Note: Đọc CLAUDE.md, MEMORY.md, docs/dev-log.md trước khi code. Cập nhật dev-log sau thay đổi. | Read these files before coding, update dev-log after changes. -->

# Session Resume — 2026-06-10 03:19:29 UTC

**Token**: `RESUME:20260610-031929-9cb811f`
**Branch**: `claude/eager-mendel-txt07k`
**HEAD**: `9cb811f75c6b2f562c6de0dc49c1cc98c8a600d8`
**Range**: `3f75af3..9cb811f` (3 commit(s))
**Author**: Claude <noreply@anthropic.com>
**Generated**: 2026-06-10T03:19:29Z

---

## 1. Summary

KPI đợt 2: reattribute atomic, bỏ creds hardcode, Làm mới tự reconcile, dedupe recon

Đợt 2 tiếp nối audit KPI buổi sáng: (1) endpoint POST /kpi-statistics/reattribute atomic (1 transaction + advisory lock theo orderCode) thay cặp DELETE→PATCH — hết race 2 recalc đồng thời, client fallback tự động khi server chưa deploy; (2) bỏ TPOS creds hardcode khỏi tab-kpi-commission.js (chuyển JSON proxy-auth {companyId}); (3) "Làm mới dữ liệu" tự recalc đơn vừa có snapshot phiếu lần đầu + reload silent — user không cần "Tính lại toàn bộ KPI" cho case phiếu xuất muộn; (4) dedupe ~80×2 dòng reconcileOne → \_buildReconRecord. Test 265 pass / 42 fail = baseline.

## 2. Files Modified

- `orders-report/js/managers/kpi-manager.js` — recalculateAndSaveKPI build statEntries[] → POST reattribute (fallback DELETE+PATCH)
- `orders-report/js/tab-kpi-commission.js` — creds → {companyId} proxy-auth; sweep recalc đơn mới có snapshot + return count; refreshData reload khi count>0; \_buildReconRecord thay 2 bản reconcileOne
- `render.com/routes/realtime-db.js` — POST /kpi-statistics/reattribute (transaction + pg_advisory_xact_lock(hashtext))
- `orders-report/*.html` ×3 — bump ?v=20260610b
- `docs/dev-log.md` — entry đợt 2

## 3. Key Decisions

- Reattribute = endpoint MỚI + client fallback flow cũ → deploy GH Pages / Render không cần đúng thứ tự. Reject: sửa in-place DELETE+PATCH (vẫn 2-3 request, vẫn race).
- Advisory lock dùng `pg_advisory_xact_lock(hashtext($1), 0)` (int4 overload, tự nhả khi COMMIT) — không cần bảng lock.
- Sweep "Làm mới" CHỈ recalc đơn nằm trong danh sách missing-snapshot (bounded, đơn chưa có phiếu → ensureKpiFinalSnapshot trả null → không recalc thừa).
- Creds: chỉ gỡ ở KPI tab (worker /api/token mode {companyId} đã chạy prod ở 7+ file). 12 file khác còn hardcode — việc riêng, đụng nhiều module.

## 4. Open Todos / Next Steps

- [ ] Sau deploy Render: bấm "Tính lại toàn bộ KPI" 1 lần (re-bucket stat_date giờ VN — từ đợt 1). Console hết log "reattribute chưa khả dụng → fallback" = endpoint mới đã live.
- [ ] Gỡ creds hardcode 12 file còn lại (hanghoan/trahang.js, banhang.js, soorder, scripts, shared token-managers legacy path...) — chuyển hết sang {companyId}.
- [ ] (Tùy chọn) loadAllStatistics fetch theo date range thay vì full lịch sử.

## 5. Context Pointers

- `docs/dev-log.md` — 2 entry 2026-06-10 (đợt 1: 9 fix; đợt 2: 4 cải tiến)
- `render.com/routes/realtime-db.js` — POST /kpi-statistics/reattribute (sau PATCH order endpoint)
- `orders-report/js/managers/kpi-manager.js` — block "── Ghi statistics ──" trong recalculateAndSaveKPI
- `orders-report/js/tab-kpi-commission.js` — \_buildReconRecord (sau \_matchRefundForOrder), \_ensureSnapshotsForVisibleOrders

## 6. Notes for Next Session

- 42 vitest fail là PRE-EXISTING (assert pattern Firestore cũ) — đã đối chiếu baseline 2 chiều cả 2 đợt.
- Flow KPI tự cập nhật realtime (logProductAction → recalc; sale-flag toggle → recalc); "Tính lại toàn bộ" chỉ cho backfill/đổi logic. "Làm mới" giờ cover case phiếu xuất sau thao tác cuối.

## 7. Previous Session (Chain Pointer)

> Một conversation có thể có nhiều token tạo dần qua các turn. Section này link đến session trước để Claude tự lần ngược, ghép đủ ngữ cảnh khi user chỉ paste **token cuối**.

- Previous: [`RESUME:20260610-030009-3f75af3`](./20260610-030009-3f75af3.md)

**Chain walking** (Claude tự làm khi nhận token):

1. Đọc file hiện tại.
2. Nếu Previous ≠ "INITIAL" → đọc thêm file previous.
3. Lặp lại tối đa **N=3 levels** mặc định (tránh context bloat).
4. Nếu user nói "đọc full chain" → đi hết tới INITIAL.

---

## How to resume

User paste vào chat mới:

```
RESUME:20260610-031929-9cb811f
```

Claude sẽ:

1. `Read` file này.
2. Walk chain theo section 7 (mặc định 3 levels, hoặc theo yêu cầu user).
3. Tóm tắt 2-3 câu để xác nhận hiểu đúng.
4. Tiếp tục từ phần "Next Steps" của session gần nhất.
