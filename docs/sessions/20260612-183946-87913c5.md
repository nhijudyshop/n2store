<!-- #Note: Đọc CLAUDE.md, MEMORY.md, docs/dev-log.md trước khi code. Cập nhật dev-log sau thay đổi. | Read these files before coding, update dev-log after changes. -->

# Session Resume — 2026-06-12 18:39:46 SEAST

**Token**: `RESUME:20260612-183946-87913c5`
**Branch**: `main`
**HEAD**: `87913c588916970501bc71bc98ca30209f1a22f7`
**Range**: `5ecfc79..87913c5` (1 commit(s))
**Author**: zxGreenxz <nguyentamca165@gmail.com>
**Generated**: 2026-06-12T11:39:46Z

---

## 1. Summary

delivery-report: ảnh bàn giao v11 — fix 3 lỗi user báo từ prod v10: (1) giá trị/Thu gửi riêng hiện 0 (Gửi Kèm nhập đơn vị NGHÌN, canvas chia /1000 lần nữa); (2) Tổng chưa cộng phần gửi riêng; (3) ảnh TMT/NAP thiếu dòng Tổng dưới cùng.

## 2. Files Modified

- `delivery-report/js/delivery-report.js` — helper `sendAlongThousand(v)` (giá trị Gửi Kèm = nghìn; ≥10.000 coi là đồng → /1000); section gửi riêng tính bằng nghìn (`extraCollectK/extraShipK/extraNetK`, hiển thị `formatNumber`); TP `grandTotal += extraNetK×1000` + `grandCount += extras.length`; TMT/NAP footer mới `Tổng — N đơn: <net + extraNetK×1000>` + Tạo lúc cùng dòng
- `delivery-report/index.html` — cache-bust `?v=20260612h`
- `docs/dev-log.md` — entry v11 (resolve conflict rebase với entries máy Mac: crm_team_id + đợt H)

## 3. Key Decisions

- Quy ước đơn vị Gửi Kèm = NGHÌN (gõ 300 = 300k, như giấy tay) — heuristic `≥ 10.000 → chia 1000` đỡ ai lỡ nhập full đồng.
- Tổng (cả 3 ảnh) giờ GỒM còn lại gửi riêng. TMT/NAP luôn hiện dòng Tổng (kể cả không gửi riêng — Tổng = Còn lại).

## 4. Open Todos / Next Steps

- [ ] User test prod sau GH Pages deploy (~3 phút), Ctrl+F5 (`v=20260612h`) — bấm lại 3 nút ảnh với data Gửi Kèm thật ngày 05/06.

## 5. Context Pointers

- `delivery-report/js/delivery-report.js` — search `sendAlongThousand` / `extraCollectK` / `grandNet`
- Chain: `./20260612-174206-23c94d1.md` (v10) → v9 → v8

## 6. Notes for Next Session

- Máy Windows KHÔNG có python3 → session/snapshot tạo TAY. Máy Mac push RẤT song song (đợt H/I web2) — commit xong phải `git pull --rebase` ngay, dev-log dễ conflict (resolve: giữ cả 2 khối entry).

## 7. Previous Session (Chain Pointer)

> Một conversation có thể có nhiều token tạo dần qua các turn. Section này link đến session trước để Claude tự lần ngược, ghép đủ ngữ cảnh khi user chỉ paste **token cuối**.

- Previous: [`RESUME:20260612-174206-23c94d1`](./20260612-174206-23c94d1.md)

**Chain walking** (Claude tự làm khi nhận token):

1. Đọc file hiện tại.
2. Nếu Previous ≠ "INITIAL" → đọc thêm file previous.
3. Lặp lại tối đa **N=3 levels** mặc định (tránh context bloat).
4. Nếu user nói "đọc full chain" → đi hết tới INITIAL.

---

## How to resume

User paste vào chat mới:

```
RESUME:20260612-183946-87913c5
```

Claude sẽ:

1. `Read` file này.
2. Walk chain theo section 7 (mặc định 3 levels, hoặc theo yêu cầu user).
3. Tóm tắt 2-3 câu để xác nhận hiểu đúng.
4. Tiếp tục từ phần "Next Steps" của session gần nhất.
