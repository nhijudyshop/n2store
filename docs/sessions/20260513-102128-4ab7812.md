<!-- #Note: Đọc CLAUDE.md, MEMORY.md, docs/dev-log.md trước khi code. Cập nhật dev-log sau thay đổi. | Read these files before coding, update dev-log after changes. -->

# Session Resume — 2026-05-13 10:21:28 +07

**Token**: `RESUME:20260513-102128-4ab7812`
**Branch**: `main`
**HEAD**: `4ab7812acbee708a0062387011d98301e71edc1b`
**Range**: `INITIAL..4ab7812` (? commit(s))
**Author**: nhijudyshop <147459165+nhijudyshop@users.noreply.github.com>
**Generated**: 2026-05-13T03:21:28Z

---

## 1. Summary

Thêm Session Resume Protocol: token `RESUME:<ts>-<sha7>` + file markdown trong git, helper script `scripts/save-session-resume.sh` auto-gen sau commit/push. Mục tiêu: nối context giữa các đoạn hội thoại Claude — token ngắn paste tay, file markdown chứa context đầy đủ.

User yêu cầu giải pháp encode/base64/hash của conversation để paste vào chat mới Claude tiếp tục. Chọn **token + file md** thay vì base64/hash thô vì hash 1-chiều không recover, base64 transcript đầy đủ vài MB không paste nổi. Đây là session đầu tiên dùng protocol — coi như smoke test luôn.

## 2. Files Modified

- `CLAUDE.md` — Thêm section "Session Resume Protocol — BẮT BUỘC" ngay sau heading chính, mô tả flow khi tạo & khi đọc token.
- `docs/dev-log.md` — Thêm section "🔗 Session Resume Protocol (BẮT BUỘC)" sau header file.
- `/Users/mac/.claude/projects/-Users-mac-Desktop-n2store/memory/MEMORY.md` — Thêm section "Session Resume Protocol (BẮT BUỘC)" lên đầu (out-of-tree, không track git).
- `docs/sessions/README.md` (NEW) — Quy ước đầy đủ: rationale, format token, khi nào tạo, khi nào đọc, 6 mục template, tools đã khảo sát.
- `docs/sessions/_TEMPLATE.md` (NEW) — Template file session với 6 mục: Header / Summary / Files / Key Decisions / Next Steps / Context Pointers.
- `scripts/save-session-resume.sh` (NEW, chmod +x) — Bash script: validate clean tree (ignore untracked), compute SHA7+timestamp, render template qua python, commit + push tự động, in token cho user.

## 3. Key Decisions

- **Format token = `RESUME:YYYYMMDD-HHMMSS-<sha7>`** (~30 ký tự, paste tay dễ).
    - Lý do: ngắn nhưng đủ unique (timestamp + git SHA). Match được bằng regex đơn giản `RESUME:[0-9]{8}-[0-9]{6}-[a-f0-9]{7}`.
    - Reject: UUID (16 ký tự hex random, không truy được commit nào), pure hash (1-chiều).

- **File markdown trong git thay vì base64/JSON state.**
    - Lý do: human-readable, audit được, git track tự đồng bộ giữa máy, search được. Đè được bất kỳ context lớn (vài MB) mà chỉ cần token ngắn để paste.
    - Reject: base64 transcript đầy đủ (paste không nổi), vector embed (over-engineer).

- **Helper script tự commit + push file session** (sau khi user/Claude commit code xong).
    - Lý do: zero friction. Claude chỉ cần gọi `bash scripts/save-session-resume.sh "<summary>"` sau khi `git push` thành công.
    - Reject: yêu cầu Claude tự ghi file thủ công (dễ quên format, dễ sai naming).

- **Untracked files được phép tồn tại khi tạo session resume.**
    - Lý do: repo có nhiều screenshot/debug artifacts trong `downloads/n2store-session/` untracked liên tục, không nên block. Chỉ check tracked changes đã commit hết.

- **Lưu vĩnh viễn trong git, không xóa cũ.**
    - Lý do: log lịch sử dev liên tục, markdown nhỏ. Archive vào `docs/sessions/archive/<năm>/` nếu cần dọn.

## 4. Open Todos / Next Steps

- [ ] **Test resume flow ở chat mới**: user mở conversation mới, paste `RESUME:20260513-102128-4ab7812`, verify Claude đọc đúng file và tiếp tục được.
- [ ] **Quan sát interaction với auto bot "auto: session update"**: bot này commit dev-log.md liên tục (khoảng mỗi vài phút). Script đã ignore untracked files OK, nhưng nếu bot commit ngay lúc script đang chạy, có thể conflict push. Hiện tại retry tay là đủ.
- [ ] **Enhance script** (optional): tự tìm `PREV_RESUME` chính xác hơn (lọc theo timestamp), tự sinh entry trong dev-log linking đến file session.
- [ ] **Document trong README dự án**: thêm "Resume Workflow" vào README root nếu cần expose ra ngoài.

## 5. Context Pointers

- `docs/sessions/README.md` — Spec đầy đủ của protocol.
- `docs/sessions/_TEMPLATE.md` — Template cho mọi session sau.
- `scripts/save-session-resume.sh` — Helper. Đọc để hiểu logic compute identifiers + render template.
- `CLAUDE.md` (head) — Section "Session Resume Protocol — BẮT BUỘC" với cả 2 chiều (khi tạo + khi đọc).
- `docs/dev-log.md` (head) — Section tương tự.
- `/Users/mac/.claude/projects/-Users-mac-Desktop-n2store/memory/MEMORY.md` (head) — Section ngắn trong project memory.

## 6. Notes for Next Session

- Auto bot `auto: session update` đang chạy: commit dev-log.md định kỳ. Khi chạy `save-session-resume.sh`, có thể phải commit dev-log trước rồi mới chạy được script. Đây là expected behavior (script validate tracked tree clean).
- Token `RESUME:20260513-102128-4ab7812` là session đầu tiên — đã commit `5d2c70b` (file session) và `4ab7812` (dev-log autocommit ngay trước).
- Nếu user paste token này → Claude `Read` file `docs/sessions/20260513-102128-4ab7812.md` và bắt đầu từ phần "Next Steps".

---

## How to resume

User paste vào chat mới:

```
RESUME:20260513-102128-4ab7812
```

Claude sẽ tự `Read` file này, tóm tắt 2-3 câu để xác nhận hiểu đúng, rồi tiếp tục từ phần "Next Steps".
