<!-- #Note: Đọc CLAUDE.md, MEMORY.md, docs/dev-log.md trước khi code. Cập nhật dev-log sau thay đổi. | Read these files before coding, update dev-log after changes. -->

# Session Resume — 2026-06-11 10:31:47 +07

**Token**: `RESUME:20260611-103147-4805aee`
**Branch**: `main`
**HEAD**: `4805aeea3`
**Author**: zxGreenxz <nguyentamca165@gmail.com>
**Generated**: 2026-06-11T03:31:47Z
**Note**: file tạo THỦ CÔNG (script save-session-resume.sh bị block vì working tree có thay đổi dở của session song song live-chat — không được commit hộ). Snapshot `docs/sessions/latest/*` sẽ tự cập nhật ở lần script chạy kế.

---

## 1. Summary

feat(showroom1): mã định danh khách vãng lai (visitor ID tăng dần từ 1) + giỏ hàng server-side + mục tra cứu "Giỏ hàng khách" trong admin panel. Khách lạ mở https://nhijudy.store/showroom1/ được tự cấp mã (#1, #2, …) hiện trên pill thay FAB chat đen; tap ảnh SP → giỏ lưu server theo mã; tap pill → bottom-sheet giỏ (stepper/xóa/tạm tính) + mã to + nút Gọi/Zalo/Messenger; khách chốt đơn bằng cách gọi/nhắn mã, shop tra giỏ realtime trong admin panel. Backend + worker đã deploy & verify prod; frontend đã test Playwright localhost đầy đủ.

## 2. Files Modified

- `render.com/routes/showroom-carts.js` — NEW route public `/api/showroom-carts` (POST /visitors cấp BIGSERIAL id + token 32-hex; GET/:id?token; PUT /:id atomic + sanitizeItems chống XSS; GET /?recent|?id cho admin; SSE topic `showroom_carts`) — đi trong commit `707692b9a` (bị commit wallet của session song song cuốn vào khi đang stage)
- `render.com/server.js` — mount + initializeNotifiers hub Web 1.0 (cùng commit trên)
- `cloudflare-worker/modules/config/routes.js` + `worker.js` — route SHOWROOM_CARTS → handleCustomer360Proxy (cùng commit trên)
- `showroom1/cart.js` — NEW: visitor ID + giỏ hàng client (register/restore/re-register, UI-first + offline cache, bottom-sheet, CONTACT placeholder)
- `showroom1/cart.css` — NEW: pill idpill, cart sheet, **fix z-index 85 cho sheet (scrim 80 chặn click)**
- `showroom1/index.html` — #chatFab → #idPill, hook add-to-cart dataset.id/price, buildCardEl set dataset, script tags + ?v
- `showroom1/admin.js` — toPreview thêm `id` (trước đây strip mất); mục "Giỏ hàng khách" + EventSource riêng `showroom_carts`
- `showroom1/admin.css` — styles `.adm-carts*`
- `docs/dev-log.md` — entry đầy đủ 2026-06-11

## 3. Key Decisions

- **Layer Web 1.0** (pool `chatDb`, SSE hub Web 1.0, route root `/api/showroom-carts`) — nhất quán với showroom-products cùng trang; KHÔNG phải Web 2.0.
  - Alternative rejected: Web 2.0 route/web2Db — showroom1 đã toàn bộ nằm bên Web 1.0, trộn 2 hub SSE trên 1 trang thêm phức tạp.
- **Visitor ID = BIGSERIAL** (race-safe, tăng từ 1) + **token 32-hex bắt buộc cho read/write giỏ của khách** vì ID tuần tự đoán được; admin list không cần token nhưng KHÔNG bao giờ trả token.
- **PUT full-replace + last-write-wins**, debounce 800ms, UI-first KHÔNG rollback khi lỗi mạng (cache localStorage + retry) — rollback giỏ trên webview Messenger/Zalo chập chờn phá ý định khách. NGOẠI LỆ có chủ đích so với quy ước Web2Optimistic (đây là Web 1.0 + lý do UX riêng).
- **EventSource riêng cho admin carts** thay vì nới SSE_URL chung của admin.js — subscribe() chạy cho cả guest, nới key sẽ làm mọi guest reload toàn bộ SP mỗi khi 1 khách sửa giỏ.
- **Chống stored-XSS 2 lớp**: server strip `<>` trong sanitizeItems + client (admin/cart) escape mọi field khi render — tên SP trong giỏ là dữ liệu khách tự gửi và render trong trang admin authed.
- Rate-limit đăng ký: in-memory 10/IP/giờ + **cap DB 500/giờ toàn cục** (phòng tuyến chính vì IP qua worker proxy không tin cậy); cleanup giỏ rỗng >30 ngày (~4% request).

## 4. Open Todos / Next Steps

- [ ] **Điền CONTACT thật** đầu `showroom1/cart.js`: `phone` + `zalo` đang placeholder `0900000000`; `messenger` đoán `m.me/NhiJudyStore` — user xác nhận/sửa. Sửa xong bump `cart.js?v=` trong index.html.
- [ ] Visitor #1–#2 đã dùng test (đã empty giỏ) → khách thật bắt đầu #3. Muốn reset về #1: `TRUNCATE showroom_carts RESTART IDENTITY` trên Render DB (n2store chatDb) TRƯỚC khi khách thật dùng.
- [ ] Verify GH Pages sau deploy (~2-4 phút sau commit `4805aeea3`): `curl -s https://nhijudy.store/showroom1/cart.js | head -3` rồi smoke prod.
- [ ] (Hardening sau, không gấp) token đang đi qua query-string GET → lộ trong log worker; chuyển sang header khi cần.

## 5. Context Pointers

- `docs/dev-log.md` — entry `### [showroom1] Mã định danh khách vãng lai` (chi tiết đầy đủ nhất)
- `render.com/routes/showroom-carts.js` — toàn bộ API + schema `showroom_carts` (lazy ensureTables)
- `showroom1/cart.js` — client logic; `showroom1/admin.js` — mục Giỏ hàng khách (cuối file, trước evaluate())
- Verify SSE: `curl -N "https://chatomni-proxy.nhijudyshop.workers.dev/api/realtime/sse?keys=showroom_carts"` + PUT ở terminal khác

## 6. Notes for Next Session

- ⚠ Có **session Claude song song** đang làm live-chat + wallet refund trên cùng working tree — khi commit phải stage ĐÍCH DANH file của mình và commit ngay (commit `707692b9a` của session kia đã cuốn 4 file backend showroom-carts đang stage của session này; vô hại nhưng message không khớp).
- Bảng `showroom_carts` tạo lazy ở request đầu — không cần migration thủ công.
- Test data convention: 2 giỏ test (#1, #2) đã empty; muốn test tiếp cứ tạo visitor mới rồi PUT items prefix `test-`.

## 7. Previous Session (Chain Pointer)

> Một conversation có thể có nhiều token tạo dần qua các turn. Section này link đến session trước để Claude tự lần ngược, ghép đủ ngữ cảnh khi user chỉ paste **token cuối**.

- Previous: [`RESUME:20260611-101806-bfd2fbd`](./20260611-101806-bfd2fbd.md) _(session song song — wallet refund/live-chat, KHÔNG cùng mạch showroom1; mạch showroom1 trước đó: [`RESUME:20260610-195235-f8a1b4f`](./20260610-195235-f8a1b4f.md))_

**Chain walking** (Claude tự làm khi nhận token):

1. Đọc file hiện tại.
2. Nếu Previous ≠ "INITIAL" → đọc thêm file previous.
3. Lặp lại tối đa **N=3 levels** mặc định (tránh context bloat).
4. Nếu user nói "đọc full chain" → đi hết tới INITIAL.

---

## How to resume

User paste vào chat mới:

```
RESUME:20260611-103147-4805aee
```

Claude sẽ:

1. `Read` file này.
2. Walk chain theo section 7 (mặc định 3 levels, hoặc theo yêu cầu user).
3. Tóm tắt 2-3 câu để xác nhận hiểu đúng.
4. Tiếp tục từ phần "Next Steps" của session gần nhất.
