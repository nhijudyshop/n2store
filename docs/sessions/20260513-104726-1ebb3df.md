<!-- #Note: Đọc CLAUDE.md, MEMORY.md, docs/dev-log.md trước khi code. Cập nhật dev-log sau thay đổi. | Read these files before coding, update dev-log after changes. -->

# Session Resume — 2026-05-13 10:47:26 +07

**Token**: `RESUME:20260513-104726-1ebb3df`
**Branch**: `main`
**HEAD**: `1ebb3dfffa4715d40217956764b04a6ff8fb860a`
**Range**: `4ab7812..1ebb3df` (8 commit(s))
**Author**: nhijudyshop <147459165+nhijudyshop@users.noreply.github.com>
**Generated**: 2026-05-13T03:47:26Z

---

## 1. Summary

issue-tracking: customer orders lookup (SĐT/tên) modal + expand

Thêm UI tra cứu nhanh tất cả đơn hàng của 1 khách (input + button đầu trang `issue-tracking/index.html`). Detect mode auto: digit-only → search phone, else → strip diacritics + search tên qua field `PartnerNameNoSign`. Mở modal liệt kê đơn với stats pills, filter tabs, range select 60/180/365/730 ngày; click row expand → load chi tiết qua `ApiService.getOrderDetails`, cache trong Map. Local Playwright verified: SĐT 0123456788 → 142 đơn, tên có/không dấu cùng kết quả 141 đơn, filter + range + expand đều hoạt động.

## 2. Files Modified

<!-- Liệt kê file + ý nghĩa thay đổi, không paste diff. Format:
- `path/to/file.js` — short why
-->

**Liên quan trực tiếp tới session này (issue-tracking customer lookup):**

- `issue-tracking/index.html` — thêm `customer-lookup-bar` (input + button "Tra cứu") dưới header + modal `modal-customer-orders` (header stats pills, filter tabs, range select, body).
- `issue-tracking/css/style.css` — +230 dòng style cho lookup bar (gradient pill design) + modal (grid layout, status pills semantic, chevron rotate, responsive <800px).
- `issue-tracking/js/customer-orders-lookup.js` **(mới, IIFE)** — toàn bộ logic: `detectMode`, `stripDiacritics`, `buildOdataFilter`, `fetchOrders` (gọi TPOS OData), `renderOrderRow`, expand click load `ApiService.getOrderDetails`, filter tabs, range re-search, ESC/click-outside close.
- `docs/dev-log.md` — entry mới mô tả implement + verify checklist.

**Carry-over từ commits trước trong range (không phải session này):**

- `orders-report/js/tab1/*` — auto-format/lint
- `tpos-pancake/js/tpos/tpos-api.js`, `tpos-customer-panel.js` — refactor remove TPOS confirmOrder/cancelOrder (commit `f0f166bf`)

## 3. Key Decisions

- **Tạo file JS riêng `customer-orders-lookup.js`** (IIFE), KHÔNG nhét vào `script.js` (4172 dòng đã quá lớn).
    - Lý do: feature độc lập, dễ maintain, không động vào ticket creation flow.
    - Expose `window.CustomerOrdersLookup` cho debug.

- **Dùng raw TPOS OData fetch** trong file mới thay vì gọi `ApiService.searchOrders`.
    - Lý do: `ApiService.searchOrders` filter `validStates = ['open', 'paid']`, đã loại bỏ cancelled/draft. Use case này cần XEM TẤT CẢ trạng thái (bao gồm hủy/nháp) để CSKH tra cứu lịch sử khách.
    - Top 200 + 180 ngày mặc định (vs ApiService dùng top 20 + 60 ngày).

- **Detect mode auto từ input** (không cần tab UI như Tạo Phiếu Mới).
    - Digit-only sau khi strip `\s.\-+` → phone mode, else → name mode.
    - +84 prefix → 0 (chuẩn hóa VN).
    - Strip diacritics (NFD + remove combining + đ→d) cho match TPOS `PartnerNameNoSign` (index không dấu của TPOS).

- **Cache details trong Map** `state.loadedDetails`, không re-fetch khi collapse/expand lại.
    - Lý do: `getOrderDetails` chậm (~500ms-1s mỗi đơn), tiết kiệm token TPOS.

## 4. Open Todos / Next Steps

- [ ] Verify deploy online: đợi GH Pages CI ~2-4 phút, mở `https://nhijudyshop.github.io/n2store/issue-tracking/index.html`, kiểm tra input "Tra cứu đơn khách" hiển thị + thử search SĐT.
- [ ] (Optional) Thêm debounce 400ms cho input + auto search khi gõ đủ 3 digits hoặc 2 chars.
- [ ] (Optional) Hiển thị thêm field `Reference` (mã đơn TPOS phụ) trong details grid nếu có.
- [ ] (Optional) Nút "Tạo phiếu cho đơn này" trong expand row → pre-fill `Tạo Phiếu Mới` modal với order đã chọn.

## 5. Context Pointers

- `issue-tracking/js/customer-orders-lookup.js` — toàn bộ logic feature (IIFE, ~450 dòng).
- `issue-tracking/index.html:60-72` — lookup bar HTML; `:556-616` — modal HTML.
- `issue-tracking/css/style.css:1009-1300` — toàn bộ CSS feature mới.
- `shared/js/api-service.js:35-157` — `searchOrders` (reference pattern, dùng OData TPOS).
- `shared/js/api-service.js:164-260` — `getOrderDetails(orderId)` (dùng cho expand).
- `shared/js/token-manager.js` — `window.tokenManager.authenticatedFetch` (auto-refresh token).
- `shared/js/api-config.js` — `window.API_CONFIG.TPOS_ODATA` = `https://chatomni-proxy.nhijudyshop.workers.dev/api/odata`.
- TPOS OData filter pattern: `(Type eq 'invoice' and IsMergeCancel ne true and DateInvoice ge {iso} and DateInvoice le {iso} and contains({Field},'{value}'))`. Field = `Phone` | `PartnerNameNoSign` | `Number`.

## 6. Notes for Next Session

- Browser session đang chạy (`scripts/n2store-browser-session.js`, PID 23904) với `--base http://localhost:8080`. FIFO: `/tmp/n2store-session.fifo`. Localhost server: `python3 -m http.server 8080` (PID 1446).
- Test customer mặc định "Huỳnh Thành Đạt — 0123456788" có 142 đơn trong 180 ngày, TẤT CẢ status `cancel` (đây là test data trên TPOS prod, không phải dữ liệu thật của khách).
- Verify online: `curl -s "https://nhijudyshop.github.io/n2store/issue-tracking/index.html" | grep -c "customer-lookup-bar"` phải trả về 1 sau khi GH Pages deploy xong.
- Edge case test: SĐT không tồn tại `0999999999` → "Không tìm thấy đơn nào...". Empty input → alert "Vui lòng nhập SĐT hoặc tên khách".
- Modal close: ESC ✓, click X ✓, click backdrop ✓. Không có outside-trigger nào re-mở modal.

---

## How to resume

User paste vào chat mới:

```
RESUME:20260513-104726-1ebb3df
```

Claude sẽ tự `Read` file này, tóm tắt 2-3 câu để xác nhận hiểu đúng, rồi tiếp tục từ phần "Next Steps".
