<!-- #Note: Đọc CLAUDE.md, MEMORY.md, docs/dev-log.md trước khi code. Cập nhật dev-log sau thay đổi. | Read these files before coding, update dev-log after changes. -->

# Session Resume — 2026-06-11 14:40:26 +07

**Token**: `RESUME:20260611-144026-13f1383`
**Branch**: `main`
**HEAD**: `13f138358`
**Author**: zxGreenxz <nguyentamca165@gmail.com>
**Generated**: 2026-06-11T07:40:26Z
**Note**: file tạo THỦ CÔNG (máy Windows không có python cho save-session-resume.sh).

---

## 1. Summary

fix(showroom1): chặn gesture "vuốt trái quay về" của webview Zalo/Messenger khi khách lướt xem ảnh SP (đặc biệt ở tấm đầu tiên vuốt qua phải bị thoát về app chat). Fix 3 lớp: (1) CSS `overscroll-behavior-x: none` html/body + `.scroll { overscroll-behavior: contain }`; (2) `touchstart {passive:false}` trên carousel card + ảnh lớn detail — touch khởi phát sát mép (<26px) → preventDefault (chặn edge-swipe hệ thống, pattern Swiper edgeSwipeDetection); (3) `touchmove {passive:false}` — đang vuốt ngang trong carousel → preventDefault (nuốt gesture). Khách quay về thì bấm mũi tên trên thanh trình duyệt.

## 2. Files Modified

- `showroom1/index.html` — CSS overscroll + touch guards trong `bindImgwrap`; bump `detail.js?v=20260611e`
- `showroom1/detail.js` — touch guards trong `bindSwipe`
- `docs/dev-log.md` — entry mới

## 3. Key Decisions

- KHÔNG dùng history-trap (`pushState` + popstate loop) — sẽ chặn luôn nút mũi tên quay về trên thanh trình duyệt, ngược ý user (muốn giữ nút đó hoạt động).
- Edge threshold 26px chỉ áp trong vùng carousel (không global) — tránh chặn nhầm scroll dọc/tap ở mép trên phần còn lại của trang.

## 4. Open Todos / Next Steps

- [ ] **User test thật trên điện thoại trong Zalo/Messenger** — gesture native của webview iOS không giả lập được bằng Playwright desktop; nếu vẫn lọt (WKWebView allowsBackForwardNavigationGestures cứng), phương án tiếp theo là history-trap có điều kiện (chỉ khi detail đang mở).
- [ ] Điền CONTACT thật đầu `showroom1/cart.js`; TRUNCATE `showroom_carts` RESTART IDENTITY (counter test ~#22); shop điền "Mô tả sản phẩm" cho từng SP.

## 5. Context Pointers

- `showroom1/index.html` — guards cuối `bindImgwrap`; CSS đầu `<style>` + rule `.scroll`
- `showroom1/detail.js` — guards cuối `bindSwipe`
- `docs/dev-log.md` — 5 entry showroom1 ngày 2026-06-11

## 6. Notes for Next Session

- ⚠ Session song song cùng working tree: localhost:8080 bị app CRM của họ chiếm → test port 8099; stage đích danh khi commit.

## 7. Previous Session (Chain Pointer)

- Previous: [`RESUME:20260611-135645-1bbcf16`](./20260611-135645-1bbcf16.md) _(cùng mạch showroom1 — trang chi tiết SP)_

**Chain walking** (Claude tự làm khi nhận token):

1. Đọc file hiện tại.
2. Nếu Previous ≠ "INITIAL" → đọc thêm file previous.
3. Lặp lại tối đa **N=3 levels** mặc định (tránh context bloat).
4. Nếu user nói "đọc full chain" → đi hết tới INITIAL.

---

## How to resume

User paste vào chat mới:

```
RESUME:20260611-144026-13f1383
```

Claude sẽ:

1. `Read` file này.
2. Walk chain theo section 7 (mặc định 3 levels, hoặc theo yêu cầu user).
3. Tóm tắt 2-3 câu để xác nhận hiểu đúng.
4. Tiếp tục từ phần "Next Steps" của session gần nhất.
