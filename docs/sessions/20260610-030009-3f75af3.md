<!-- #Note: Đọc CLAUDE.md, MEMORY.md, docs/dev-log.md trước khi code. Cập nhật dev-log sau thay đổi. | Read these files before coding, update dev-log after changes. -->

# Session Resume — 2026-06-10 03:00:09 UTC

**Token**: `RESUME:20260610-030009-3f75af3`
**Branch**: `claude/eager-mendel-txt07k`
**HEAD**: `3f75af39dcac3576bba76cc632437aa43b025ae0`
**Range**: `daf21ec..3f75af3` (6 commit(s))
**Author**: Claude <noreply@anthropic.com>
**Generated**: 2026-06-10T03:00:09Z

---

## 1. Summary

Rà soát hệ thống KPI đơn đánh giá: fix timezone stat_date VN, audit log idempotency, double render, strip details payload, cache employee-ranges

Audit toàn bộ pipeline KPI upselling (BASE → audit log → NET=final−BASE → statistics → dashboard đối soát). Tìm thấy 9 lỗi logic/hiệu suất và đã fix: timezone bucket stat_date (UTC→VN), audit log ghi trùng khi offline flush (thêm clientId idempotency), saveKPIStatistics drop legacy fields, leaderboard cộng cả đơn stale, invoice cache không refresh, double render mỗi lần lọc, payload kpi-statistics chứa details không ai đọc, employee-ranges fetch lặp N×2 khi recompute, DELETE trùng trong recomputeAllKPI. Vitest: 265 pass, 42 fail GIỐNG HỆT baseline (pre-existing, không regression).

## 2. Files Modified

- `orders-report/js/managers/kpi-manager.js` — vnDateString() bucket stat_date theo giờ VN; forward netProductsLegacy/kpiLegacy trong saveKPIStatistics; cache employee_ranges TTL 60s
- `orders-report/js/managers/kpi-audit-logger.js` — sinh clientId UUID per log entry (idempotency)
- `orders-report/js/tab-kpi-commission.js` — bỏ double render + dead loadInboxKpiStats trong applyFilters/init; aggregateByEmployee loại \_stale; refreshData reload invoice cache; recomputeAllKPI bỏ probe + DELETE trùng; exportExcel align tiêu chí đếm đơn
- `render.com/routes/realtime-db.js` — client_id column + partial unique index (lazy ensure) + ON CONFLICT DO NOTHING (POST đơn + batch); GET /kpi-statistics strip details; list-meta filter ngày theo Asia/Ho_Chi_Minh
- `orders-report/tab-kpi-commission.html`, `tab1-orders.html`, `migration-kpi-per-user.html` — bump cache-bust ?v=20260610a
- `docs/dev-log.md` — entry chi tiết 2026-06-10 (9 fix + danh sách lỗi chưa sửa)
- (các file tab1-fast-sale\*/processing-tags/tpos-realtime/kpi-reconciled-net.test.js trong range là của các commit trước trên branch, không thuộc phiên này)

## 3. Key Decisions

- stat_date bucket = **giờ VN cố định (UTC+7)** qua `vnDateString()`, KHÔNG dùng TZ máy user → deterministic kể cả khi admin chạy "Tính lại" từ máy TZ khác. Server list-meta dùng `AT TIME ZONE 'Asia/Ho_Chi_Minh'` cho khớp. Reject: giữ UTC (lệch ngày 00:00–07:00 VN, sai kỳ lương mép tháng).
- Idempotency audit log = **client_id + partial unique index + ON CONFLICT DO NOTHING**, lazy-ensure column (pattern giống ensureKpiFinalSnapshotTable, không cần migration file riêng). Cột nullable → backward-compat client cũ. Reject: nới dedup 5s theo content (vẫn miss flush muộn, lại chặn add hợp lệ).
- Strip `details` CHỈ ở endpoint LIST /kpi-statistics — đã verify 0 consumer đọc details từ list (dashboard + tab1 strip tính live qua calculateNetKPI); endpoint per-(user,date) giữ nguyên.
- `loadInboxKpiStats` là dead code path (\_inboxKpiByUser/\_getInboxKpiForUser không còn caller) → bỏ gọi nhưng GIỮ method cho backward-compat.
- KHÔNG đụng: hardcoded TPOS creds (systemic 13 files — cần fix tận gốc ở worker /api/token), race DELETE→PATCH 2 recalc đồng thời (cần endpoint reattribute atomic), out_of_range dead flag, duplicate reconcileOne (~80 dòng recon global vs L1).

## 4. Open Todos / Next Steps

- [ ] Sau deploy Render: mở tab KPI → bấm "Tính lại toàn bộ KPI" để re-bucket stat_date theo giờ VN (đơn BASE 00:00–07:00 dồn về đúng ngày).
- [ ] Verify prod: `curl .../api/realtime/kpi-statistics | jq '.statistics[0].orders[0] | keys'` → không còn `details`; check log Render có `idx_kpi_audit_client_id` ensure OK.
- [ ] (Đề xuất, chưa làm) Gom DELETE+PATCH kpi-statistics thành 1 endpoint reattribute atomic (transaction) để hết race 2 recalc đồng thời cùng đơn.
- [ ] (Đề xuất, chưa làm) Bỏ hardcoded TPOS creds khỏi client JS (13 files) — worker /api/token nên tự inject creds server-side.
- [ ] (Đề xuất) loadAllStatistics đang fetch FULL lịch sử kpi_statistics — cân nhắc fetch theo dateFrom/dateTo + refetch khi đổi range (sau khi strip details thì áp lực giảm nhiều).

## 5. Context Pointers

- `docs/dev-log.md` — entry 2026-06-10 (full danh sách 9 fix + lỗi chưa sửa)
- `orders-report/js/managers/kpi-manager.js` — vnDateString ~L206, saveKPIStatistics ~L1095, recalculateAndSaveKPI ~L1160, \_fetchEmployeeRangesCached ~L1310
- `render.com/routes/realtime-db.js` — ensureKpiAuditClientIdColumn ~L938, POST /kpi-audit-log ~L957, GET /kpi-statistics strip ~L1524, list-meta VN_DATE ~L640
- `orders-report/js/tab-kpi-commission.js` — applyFilters ~L1208, aggregateByEmployee ~L2216, recomputeAllKPI ~L5480, refreshData ~L5540
- `orders-report/docs/KPI_CALCULATION_GUIDE.md` — guide flow KPI (lưu ý: doc cũ còn mô tả Firebase, code thật 100% Render PG)

## 6. Notes for Next Session

- 42 test vitest fail là PRE-EXISTING (assert pattern Firestore/runTransaction đã bỏ từ lâu) — đối chiếu `git stash` 2 chiều xác nhận. Đừng hoảng khi thấy fail.
- `kpi_statistics.orders[].details` vẫn ĐƯỢC GHI khi recalc (chỉ strip ở response LIST) — nếu muốn giảm dung lượng DB thì cân nhắc bỏ ghi, nhưng phải check lại consumer tương lai.
- Sau fix timezone, đơn cũ stat_date UTC vẫn nằm bucket cũ cho tới khi "Tính lại KPI" (recalc tự DELETE row cũ mọi nơi rồi ghi lại đúng ngày VN).

## 7. Previous Session (Chain Pointer)

> Một conversation có thể có nhiều token tạo dần qua các turn. Section này link đến session trước để Claude tự lần ngược, ghép đủ ngữ cảnh khi user chỉ paste **token cuối**.

- Previous: [`RESUME:20260609-195135-daf21ec`](./20260609-195135-daf21ec.md)

**Chain walking** (Claude tự làm khi nhận token):

1. Đọc file hiện tại.
2. Nếu Previous ≠ "INITIAL" → đọc thêm file previous.
3. Lặp lại tối đa **N=3 levels** mặc định (tránh context bloat).
4. Nếu user nói "đọc full chain" → đi hết tới INITIAL.

---

## How to resume

User paste vào chat mới:

```
RESUME:20260610-030009-3f75af3
```

Claude sẽ:

1. `Read` file này.
2. Walk chain theo section 7 (mặc định 3 levels, hoặc theo yêu cầu user).
3. Tóm tắt 2-3 câu để xác nhận hiểu đúng.
4. Tiếp tục từ phần "Next Steps" của session gần nhất.
