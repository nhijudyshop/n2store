<!-- #Note: Đọc CLAUDE.md, MEMORY.md, docs/dev-log.md trước khi code. Cập nhật dev-log sau thay đổi. | Read these files before coding, update dev-log after changes. -->

# Session Resume — 2026-06-28 16:39:07 +07

**Token**: `RESUME:20260628-163907-7a639f3`
**Branch**: `main`
**HEAD**: `7a639f39fdfabe1e337df45a34d71a495336d932`
**Range**: `d769dda..7a639f3` (2 commit(s))
**Author**: nhijudyshop <147459165+nhijudyshop@users.noreply.github.com>
**Generated**: 2026-06-28T09:39:07Z

---

## 1. Summary

so-order money S2-S5 (Tab Đợt + stat cards trên bảng + chi phí đợt + thanh toán CK ledger) + bỏ cột Ghi Chú CP

Hoàn tất 4/4 phần money-plan (DESIGN LOCKED 27/06, S1 đã xong phiên trước): S2 dải tab Đợt cấp 2, S3 stat cards KG/HĐ/CP/TT/CÒN LẠI theo đợt, S4 Chi phí đợt inline trong Sửa lô, S5 Thanh toán CK → ghi ledger NCC realtime. Sau đó user yêu cầu 2 tweak: chuyển stat cards LÊN TRÊN bảng + bỏ cột "Ghi Chú CP" (thay bằng feature Chi phí đợt). Verify qua browser (login web2 từ secret): S2/S3 data thật khớp, S4/S5 fake-state writes stubbed (zero prod write). Commit `7a639f3` đã push.

## 2. Files Modified

<!-- Liệt kê file + ý nghĩa thay đổi, không paste diff. Format:
- `path/to/file.js` — short why
-->

- `docs/dev-log.md` — _(điền lý do)_
- `docs/sessions/20260628-155348-d769dda.md` — _(điền lý do)_
- `docs/sessions/latest/_all.md` — _(điền lý do)_
- `docs/sessions/latest/docs.md` — _(điền lý do)_
- `docs/sessions/latest/so-order.md` — _(điền lý do)_
- `docs/web2/SO-ORDER-MONEY-PLAN.md` — _(điền lý do)_
- `so-order/css/so-order.css` — _(điền lý do)_
- `so-order/index.html` — _(điền lý do)_
- `so-order/js/so-order-app.js` — _(điền lý do)_
- `so-order/js/so-order-modal-core.js` — _(điền lý do)_
- `so-order/js/so-order-modal-open.js` — _(điền lý do)_
- `so-order/js/so-order-modal-submit.js` — _(điền lý do)_
- `so-order/js/so-order-payments.js` — _(điền lý do)_
- `so-order/js/so-order-render.js` — _(điền lý do)_
- `so-order/js/so-order-shipment.js` — _(điền lý do)_
- `so-order/js/so-order-state.js` — _(điền lý do)_
- `so-order/js/so-order-storage.js` — _(điền lý do)_

## 3. Key Decisions

- **Đợt = nhóm shipment theo `sh.batch`** (KHÔNG = 1 shipment). Đợt mới nhất (max date) ở đầu, "Tất cả" cuối; dải tự ẩn khi <2 đợt. `activeBatch` per-device per-tab (localStorage map `soOrder_activeBatch_v1` + sentinel `ALL_BATCH=' ALL'`), KHÔNG sync server (như activeTabId). hasOwnProperty check để batchKey '' (Chưa đặt đợt) không rơi nhầm về ALL.
- **TỔNG TT đọc từ ledger NCC** (`web2_supplier_ledger` via `/api/web2-supplier-wallet/state`), lọc `tx.ref.source==='so-order'` → `SO._paymentsByShipment` Map<shipmentId,vnd>. Payment gắn `ref={source,tabId,batch,shipmentId,ncc}`; `ref.batch` = batch THẬT của lô được gắn (kể cả xem "Tất cả").
- **Money op GIỮ await+loading+rollback** (CLAUDE.md rule 8 ngoại lệ) — KHÔNG Web2Optimistic. txId idempotent sinh lúc mở modal.
- **Bỏ cột Ghi Chú CP (costNote)**: chỉ gỡ khỏi `SO.COLUMNS` + ô modal; data costNote cũ trên row GIỮ NGUYÊN (không render, không xoá) — reversible. 3 read `form.elements.costNote` đã guard `if (...)`.
- **Stat cards đặt TRÊN bảng** (user yêu cầu "di chuyển lên trên") — section order: tab-strip → batch-strip → stat-strip → table.
- **renderFooterTotals giữ tên** (renderAll + bulk-edit gọi) nhưng body giờ render stat cards — tránh churn.

## 4. Open Todos / Next Steps

- [ ] (Optional) Verify REAL ledger round-trip + cross-page Công nợ NCC bằng 1 payment NCC test (vd `TEST-NCC`) trên prod web2 — hiện mới verify by-construction (cùng endpoint `/tx` như supplier-debt recordPayment). Web 2.0 beta wipeable.
- [ ] (Optional) CP cho lô đã NHẬN ĐỦ: hiện không vào được modal Sửa lô (openShipmentModal chặn khi all-received) → không thêm CP muộn (customs/ship cuối). Cân nhắc entry point CP riêng nếu user cần.
- [ ] (Optional) Khi đợt nhiều shipment, payment chỉ gắn 1 shipmentId (đúng tổng đợt, lệch breakdown per-shipment) — chấp nhận được vì stat ở cấp đợt.

## 5. Context Pointers

- `docs/web2/SO-ORDER-MONEY-PLAN.md` — tracker S1-S6 (đã tick hết) + nguồn file:line
- `so-order/js/so-order-payments.js` — module S5 (load/record/modal/SSE)
- `so-order/js/so-order-render.js` — `batchGroups`/`renderBatchStrip`/`getBatchTotals`/`renderFooterTotals`(=stat cards)
- `so-order/js/so-order-storage.js` — batch view-state API (`ALL_BATCH`/`batchKeyOf`/`getActiveBatch`/`setActiveBatch`) + S1 expense APIs
- `so-order/js/so-order-shipment.js` — expense UI (`_renderShipmentExpenses`/`wireExpensesEditor`)
- Browser test: web2 login phải có token (`web2_auth`), so-order guard bounce nếu thiếu. Login: secret line "Account browser test web 2.0" → `/api/web2-users/login`. Helper: `scratchpad/web2-login.js`.

## 6. Notes for Next Session

<!-- Cảnh báo, gotcha, dependency cần check, env state, browser session đang chạy, etc. -->

## 7. Previous Session (Chain Pointer)

> Một conversation có thể có nhiều token tạo dần qua các turn. Section này link đến session trước để Claude tự lần ngược, ghép đủ ngữ cảnh khi user chỉ paste **token cuối**.

- Previous: [`RESUME:20260628-155348-d769dda`](./20260628-155348-d769dda.md)

**Chain walking** (Claude tự làm khi nhận token):

1. Đọc file hiện tại.
2. Nếu Previous ≠ "INITIAL" → đọc thêm file previous.
3. Lặp lại tối đa **N=3 levels** mặc định (tránh context bloat).
4. Nếu user nói "đọc full chain" → đi hết tới INITIAL.

---

## How to resume

User paste vào chat mới:

```
RESUME:20260628-163907-7a639f3
```

Claude sẽ:

1. `Read` file này.
2. Walk chain theo section 7 (mặc định 3 levels, hoặc theo yêu cầu user).
3. Tóm tắt 2-3 câu để xác nhận hiểu đúng.
4. Tiếp tục từ phần "Next Steps" của session gần nhất.
