<!-- #Note: Đọc CLAUDE.md, MEMORY.md, docs/dev-log.md trước khi code. Cập nhật dev-log sau thay đổi. | Read these files before coding, update dev-log after changes. -->

# Session Resume — 2026-06-11 17:35:56 +07

**Token**: `RESUME:20260611-173556-0686f08`
**Branch**: `main`
**HEAD**: `0686f088cfd5a14aa2325b7356c42dcac31002a0`
**Range**: `651a211..0686f08` (2 commit(s))
**Author**: zxGreenxz <nguyentamca165@gmail.com>
**Generated**: 2026-06-11T10:35:56Z

---

## 1. Summary

Lien thong CSKH -> delivery-report: xuat excel Thu ve 2 cot SL/Gia tri tu ticket RETURN_SHIPPER + danh dau ban giao ship (idempotent theo so don)

Khi bấm Xuất excel ở tab "Thu về" (tra soát) bên Thống Kê Giao Hàng: theo SĐT khách tìm ticket CSKH `RETURN_SHIPPER` + `PENDING_GOODS` mới nhất chưa bàn giao → thêm 2 cột "Số lượng"/"Giá trị" (giá gốc × SL thu về) vào Excel, đồng thời server đánh dấu ticket đã bàn giao thu về cho ship (handover_at + gắn số đơn). Xuất lại lần nữa → lookup theo `handover_order_number` trả đúng ticket cũ, không bốc ticket mới hơn.

## 2. Files Modified

<!-- Liệt kê file + ý nghĩa thay đổi, không paste diff. Format:
- `path/to/file.js` — short why
-->

- `render.com/migrations/076_ticket_handover_to_shipper.sql` — NEW: 4 cột `handover_*` trên `customer_tickets` + unique partial index `handover_order_number` (neo idempotency) + index claim path; additive + idempotent
- `render.com/routes/v2/tickets.js` — NEW endpoint `POST /handover-batch` (trước `GET /:id`) + `ensureHandoverSchema` lazy bootstrap (pattern wallet-refund 075) + helper `computeTicketReturnTotals`
- `delivery-report/js/delivery-report.js` — NEW `fetchReturnHandoverInfo` + `buildExcelRowsReturn` (8 cột) + hook `exportExcel()` chỉ khi tab `return`; API lỗi → alert + xuất 6 cột như cũ
- `shared/js/api-service.js` — alias `handoverAt/handoverOrderNumber/handoverBy` vào 3 ticket mappers (search/subscribe/getTicket)
- `issue-tracking/js/script.js` — badge tím "🚚 Bàn giao ship" trên row RETURN_SHIPPER + step "Bàn giao ship" trong `buildTicketTimeline`
- `docs/dev-log.md` — entry mới (kèm resolve conflict rebase với session Mac)
- `docs/sessions/*` — session chain + latest snapshots

## 3. Key Decisions

- Chỉ match ticket type `RETURN_SHIPPER` (user chốt — không gồm `RETURN_CLIENT`/`BOOM` dù cùng Chờ Hàng Về).
- "Giá trị" = giá gốc `price × returnQuantity` (user chốt "tổng giá gốc") — KHÔNG dùng giá sau giảm note như cách tính hoàn ví CSKH.
- CHỈ nút Xuất excel ở tab Thu về; sheet THUVE trong xuất "Tất cả"/Tỉnh/0đ giữ 6 cột — tránh side-effect đánh dấu bàn giao khi xuất tab khác.
- Idempotency neo theo `handover_order_number` (unique partial index): 1 số đơn giao hàng = 1 ticket mãi mãi; lookup trước, claim sau. `pg_advisory_xact_lock(hashtext(order_number))` chống 2 export đồng thời.
- Phone: client gửi RAW, server normalize bằng `utils/customer-helpers.normalizePhone` (giữ số 0 đầu, khớp format `customer_tickets.phone`). Reject dùng `normalizePhone` local của delivery-report (strip số 0 đầu → lệch).
- Schema apply qua lazy bootstrap trong tickets.js (máy Windows không có psql/python3 để chạy migration tay) — migration additive `IF NOT EXISTS` nên an toàn chạy mỗi cold start.

## 4. Open Todos / Next Steps

- [ ] Đợi Render deploy commit `0686f08` (build filter: commit chạm `render.com/**` → có build) rồi verify prod API: tạo ticket TEST `RETURN_SHIPPER` cho SĐT `0123456788` → POST `/api/v2/tickets/handover-batch` với order_number `TEST-HANDOVER-*` → check quantity/value + idempotent (POST lại = `already_handed_over: true`, tạo ticket mới hơn vẫn trả ticket cũ) → cleanup ticket TEST.
- [ ] Verify GH Pages deploy frontend (~3 phút) → smoke tab Thu về trên https://nhijudy.store/delivery-report/ (KHÔNG bấm export với đơn thật khi chưa muốn đánh dấu — export thật sẽ claim ticket thật).
- [ ] Cân nhắc (chưa yêu cầu): nút un-mark bàn giao trên CSKH nếu kế toán xuất nhầm.

## 5. Context Pointers

- `docs/dev-log.md` — entry "[delivery-report][issue-tracking][render] Liên thông CSKH → Xuất excel Thu về"
- `render.com/routes/v2/tickets.js` — route `POST /handover-batch` (sau `GET /stats`)
- `delivery-report/js/delivery-report.js:1650-1715` — fetchReturnHandoverInfo + buildExcelRowsReturn
- `render.com/migrations/076_ticket_handover_to_shipper.sql`
- Plan đã duyệt: `C:\Users\Nguyen Tam\.claude\plans\trang-cskh-l-n-i-imperative-wolf.md`

## 6. Notes for Next Session

- Máy Windows này KHÔNG có python3 → `scripts/save-session-resume.sh` fail ở 2 block python; session file này sinh bằng node replica (temp). Cân nhắc port script sang node cho cross-platform.
- Session Mac song song đang làm Render infra (build minutes, livestream migrate) — push liên tục, rebase trước khi push.
- Render workspace từng hết build minutes (đã set spend limit + build filters) — nếu deploy không chạy, check Dashboard.

## 7. Previous Session (Chain Pointer)

> Một conversation có thể có nhiều token tạo dần qua các turn. Section này link đến session trước để Claude tự lần ngược, ghép đủ ngữ cảnh khi user chỉ paste **token cuối**.

- Previous: [`RESUME:20260611-173140-651a211`](./20260611-173140-651a211.md)

**Chain walking** (Claude tự làm khi nhận token):

1. Đọc file hiện tại.
2. Nếu Previous ≠ "INITIAL" → đọc thêm file previous.
3. Lặp lại tối đa **N=3 levels** mặc định (tránh context bloat).
4. Nếu user nói "đọc full chain" → đi hết tới INITIAL.

---

## How to resume

User paste vào chat mới:

```
RESUME:20260611-173556-0686f08
```

Claude sẽ:

1. `Read` file này.
2. Walk chain theo section 7 (mặc định 3 levels, hoặc theo yêu cầu user).
3. Tóm tắt 2-3 câu để xác nhận hiểu đúng.
4. Tiếp tục từ phần "Next Steps" của session gần nhất.
