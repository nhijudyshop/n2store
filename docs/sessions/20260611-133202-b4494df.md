<!-- #Note: Đọc CLAUDE.md, MEMORY.md, docs/dev-log.md trước khi code. Cập nhật dev-log sau thay đổi. | Read these files before coding, update dev-log after changes. -->

# Session Resume — 2026-06-11 13:32:02 +07

**Token**: `RESUME:20260611-133202-b4494df`
**Branch**: `main`
**HEAD**: `b4494df70`
**Author**: zxGreenxz <nguyentamca165@gmail.com>
**Generated**: 2026-06-11T06:32:02Z
**Note**: file tạo THỦ CÔNG (máy Windows không có python cho save-session-resume.sh).

---

## 1. Summary

feat(showroom1): animation "món hàng bay vào giỏ" — khi khách thêm SP (đặc biệt SP không có size/màu vốn không có dấu hiệu gì), một viên tròn 46px chứa ảnh SP bay theo đường cong từ card → pill giỏ đen dưới cùng, hạ cánh thì pill nảy lên. Gắn vào cả 2 đường: thêm thẳng (SP không variant, bay từ `.imgwrap`) + sau confirm picker size/màu (bay từ `#pickThumb`). Đã verify Playwright localhost + prod (visitor #19, dọn sạch).

## 2. Files Modified

- `showroom1/cart.js` — `flyToCart(sourceEl, imageUrl)` (WAAPI 650ms, midpoint +44px tạo vòng cung, scale-aware cho admin preview, prefers-reduced-motion → chỉ pulse, fallback setTimeout 900ms), `pulsePill()` tách riêng
- `showroom1/cart.css` — `.fly-item` (z-index 95) + `.fly-dot` fallback SP không ảnh
- `showroom1/index.html` — `bindAddBag` truyền `.imgwrap` làm sourceEl; bump `?v=20260611c`
- `docs/dev-log.md` — entry mới

## 3. Key Decisions

- Animation chạy cho CẢ đường picker (không chỉ SP không variant như user nêu) — feedback nhất quán, chi phí 1 dòng.
- Flyer append vào `.screen` (absolute, local coords quy đổi theo scale) — không dùng position:fixed vì admin preview transform scale phone làm fixed lệch.

## 4. Open Todos / Next Steps

(Giữ nguyên từ session trước — chưa làm:)

- [ ] **Điền CONTACT thật** đầu `showroom1/cart.js` (phone/zalo placeholder `0900000000`, messenger đoán `m.me/NhiJudyStore`) → bump `cart.js?v=`.
- [ ] **TRUNCATE showroom_carts RESTART IDENTITY** trên Render DB trước khi gửi link cho khách thật — counter test đã chạy tới #19, giỏ #6 còn item test không xóa được (mất token).
- [ ] (Hardening) token qua query-string GET → chuyển header.

## 5. Context Pointers

- `showroom1/cart.js` — `flyToCart` ngay sau `pulsePill` (đầu file, sau updatePill)
- `docs/dev-log.md` — 3 entry showroom1 ngày 2026-06-11 (v1 visitor ID, v2 UX, v3 animation)

## 6. Notes for Next Session

- ⚠ Session Claude song song trên cùng working tree (live-chat/wallet) — WIP của họ có thể ở `stash@{0}`; khi commit stage đích danh file của mình + commit ngay.
- Máy Windows này không có python → tạo session file thủ công.

## 7. Previous Session (Chain Pointer)

- Previous: [`RESUME:20260611-113044-ecc0c4c`](./20260611-113044-ecc0c4c.md) _(cùng mạch showroom1 — UX v2 nút giỏ + picker size/màu)_

**Chain walking** (Claude tự làm khi nhận token):

1. Đọc file hiện tại.
2. Nếu Previous ≠ "INITIAL" → đọc thêm file previous.
3. Lặp lại tối đa **N=3 levels** mặc định (tránh context bloat).
4. Nếu user nói "đọc full chain" → đi hết tới INITIAL.

---

## How to resume

User paste vào chat mới:

```
RESUME:20260611-133202-b4494df
```

Claude sẽ:

1. `Read` file này.
2. Walk chain theo section 7 (mặc định 3 levels, hoặc theo yêu cầu user).
3. Tóm tắt 2-3 câu để xác nhận hiểu đúng.
4. Tiếp tục từ phần "Next Steps" của session gần nhất.
