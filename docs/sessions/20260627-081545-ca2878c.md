<!-- #Note: Đọc CLAUDE.md, MEMORY.md, docs/dev-log.md trước khi code. Cập nhật dev-log sau thay đổi. | Read these files before coding, update dev-log after changes. -->

# Session Resume — 2026-06-27 08:15:45 +07

**Token**: `RESUME:20260627-081545-ca2878c`
**Branch**: `main`
**HEAD**: `ca2878c46dc56d6184b8d10922173ef82c1b610b`
**Range**: `fb697db..ca2878c` (3 commit(s))
**Author**: nhijudyshop <147459165+nhijudyshop@users.noreply.github.com>
**Generated**: 2026-06-27T01:15:45Z

---

## 1. Summary

web2 flow R3: fix #2 khoá kỳ lương server-side (7 route 409) + #5 cashbook biên ngày sub-second; verify đối kháng 5 false-positive; restore dev-log xoá nhầm 539 dòng

<!-- 2-4 câu mở rộng: phiên này đã làm gì, vì sao, kết quả ra sao -->

## 2. Files Modified

<!-- Liệt kê file + ý nghĩa thay đổi, không paste diff. Format:
- `path/to/file.js` — short why
-->

- `docs/dev-log.md` — _(điền lý do)_
- `docs/sessions/20260627-080702-fb697db.md` — _(điền lý do)_
- `docs/sessions/latest/_all.md` — _(điền lý do)_
- `docs/sessions/latest/render.com.md` — _(điền lý do)_
- `docs/web2/FLOW-AUDIT-2026-06-26-R3.md` — _(điền lý do)_
- `render.com/lib/web2-cashbook-lib.js` — _(điền lý do)_
- `render.com/routes/web2-cashbook.js` — _(điền lý do)_

## 3. Key Decisions

- **R3 = vòng verify ĐỐI KHÁNG, KHÔNG sửa mù**: 8 finding R3 → chỉ 3 thật (#1,#2,#5), 5 còn lại false-positive/fix-sẽ-regress. Quyết định KHÔNG "fix" #6/#7 vì kiểm code thật cho thấy sửa sẽ gây regression:
    - #6 payroll override: FE `cham-cong-payroll.js:677-679` LUÔN gửi đủ 3 field override (null=xoá chủ ý). Server set thẳng `=$9` là ĐÚNG. COALESCE-on-omit → không xoá được override → regress.
    - #7 Excel count: `inserted` đếm cả ON CONFLICT UPDATE là cosmetic. Fix `RETURNING (xmax=0)` sẽ khiến punch sửa giờ (cùng id, đổi check_time) → inserted=0 → KHÔNG fire `_notify` → tab khác không refresh. Giữ nguyên.
- **#2 khoá kỳ lương**: guard `isMonthLocked(db, monthKey)` **fail-open** khi bảng lock chưa migrate (tránh chặn nhầm). Agent ingest nền (`/records/bulk`, ADMS) KHÔNG chặn — snapshot đã đông cứng. Chỉ chặn 7 mutation admin TAY.
- **#5 cashbook**: biên cuối EXCLUSIVE `< (end+1 ngày)T00:00+07` thay `<= 23:59:59` (sub-second-safe; VN không DST nên +86400000ms an toàn).
- **dev-log restore**: working tree session trước xoá nhầm 539 dòng (section 2026-06-19, 0 thêm) → `git checkout HEAD -- docs/dev-log.md` rồi append entry R3 sạch. KHÔNG commit deletion.

## 4. Open Todos / Next Steps

- [x] R3 #2 (khoá kỳ lương server-side) — FIXED + 18 assertions.
- [x] R3 #5 (cashbook biên ngày) — FIXED + 7 assertions.
- [x] R3 #3/#4/#6/#7/#8 — verify đối kháng, ghi lý do non-bug trong doc R3.
- [ ] **Audit R4 (tuỳ chọn)**: luồng CHƯA soi sâu R1-R3 — voucher Web 1.0 `soquy_vouchers`, attendance payroll snapshot công thức (`cham-cong-salary.js:238-239` vs `validateLockSnapshot`), AI KOL billing. Nếu user muốn tiếp tục audit.
- [ ] (LOW, optional) UI cham-cong: surface 409 từ guard khoá kỳ thành toast rõ ràng (hiện đã có `cc.toast(e.message)` ở payroll save — verify các mutation khác cũng show).

## 5. Context Pointers

- Doc audit: `docs/web2/FLOW-AUDIT-2026-06-26-R3.md` (R3, 8 finding) + R1/R2 cùng thư mục.
- Fix #2: `render.com/routes/web2-attendance.js` (helper `isMonthLocked` + `LOCK_MSG` ~line 491; guards ở POST/DELETE records, PUT payroll, POST/DELETE fullday, POST/DELETE holidays).
- Fix #5: `render.com/routes/web2-cashbook.js` (/summary ~568, /report ~620) + `render.com/lib/web2-cashbook-lib.js` (`buildVoucherFilter` ~183).
- Test harness (scratchpad, KHÔNG commit): `lock-test.js` (18), `cashbook-test.js` (7), `tags-test.js` (9). Pattern: mount route thật + Postgres `n2store_flow_test` + stub `authMod.requireWeb2Admin` TRƯỚC require route.
- Test DB: `n2store_flow_test` (local, host /tmp). `ensureSchema(pool)` gọi trực tiếp (route không có /health).

## 6. Notes for Next Session

- ⚠ `requireWeb2Admin` là HARD gate (luôn 401 nếu thiếu token, KHÔNG phụ thuộc WEB2_AUTH_ENFORCE). Test phải stub `authMod.requireWeb2Admin` TRƯỚC khi require route (route destructure tại load-time).
- ⚠ `insertRecords` chặn punch tương lai > 36h (`maxTs`). Test seed punch phải dùng ngày quá khứ/hiện tại (today=2026-06-27).
- Test DB `n2store_flow_test` còn sống — drop khi xong nếu cần: `dropdb n2store_flow_test`.
- Browser session env (nếu cần): `scratchpad/sess.env` (FIFO=/tmp/n2s-10441.fifo PORT=9903) — có thể đã chết.

## 7. Previous Session (Chain Pointer)

> Một conversation có thể có nhiều token tạo dần qua các turn. Section này link đến session trước để Claude tự lần ngược, ghép đủ ngữ cảnh khi user chỉ paste **token cuối**.

- Previous: [`RESUME:20260627-080702-fb697db`](./20260627-080702-fb697db.md)

**Chain walking** (Claude tự làm khi nhận token):

1. Đọc file hiện tại.
2. Nếu Previous ≠ "INITIAL" → đọc thêm file previous.
3. Lặp lại tối đa **N=3 levels** mặc định (tránh context bloat).
4. Nếu user nói "đọc full chain" → đi hết tới INITIAL.

---

## How to resume

User paste vào chat mới:

```
RESUME:20260627-081545-ca2878c
```

Claude sẽ:

1. `Read` file này.
2. Walk chain theo section 7 (mặc định 3 levels, hoặc theo yêu cầu user).
3. Tóm tắt 2-3 câu để xác nhận hiểu đúng.
4. Tiếp tục từ phần "Next Steps" của session gần nhất.
