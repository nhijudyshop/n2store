<!-- #Note: Đọc CLAUDE.md, MEMORY.md, docs/dev-log.md trước khi code. Cập nhật dev-log sau thay đổi. | Read these files before coding, update dev-log after changes. -->

# Session Resume — 2026-06-23 18:45:56 +07

**Token**: `RESUME:20260623-184556-af2ca38`
**Branch**: `main`
**HEAD**: `af2ca38c66dd4981609bcb445236ebc0b4dd9394`
**Range**: `80cfd2d..af2ca38` (2 commit(s))
**Author**: nhijudyshop <147459165+nhijudyshop@users.noreply.github.com>
**Generated**: 2026-06-23T11:45:56Z

---

## 1. Summary

web2 money-flow audit: cost-cap hoàn NCC server-side + cart race lock + refund SSE web2:products (3 bug verified)

<!-- 2-4 câu mở rộng: phiên này đã làm gì, vì sao, kết quả ra sao -->

## 2. Files Modified

<!-- Liệt kê file + ý nghĩa thay đổi, không paste diff. Format:
- `path/to/file.js` — short why
-->

- `docs/dev-log.md` — _(điền lý do)_
- `docs/sessions/20260623-183215-80cfd2d.md` — _(điền lý do)_
- `docs/sessions/latest/_all.md` — _(điền lý do)_
- `docs/sessions/latest/docs.md` — _(điền lý do)_
- `docs/sessions/latest/render.com.md` — _(điền lý do)_
- `docs/sessions/latest/web2.md` — _(điền lý do)_
- `render.com/lib/web2-so-order-qty.js` — _(điền lý do)_
- `render.com/routes/purchase-refund.js` — _(điền lý do)_
- `render.com/routes/v2/cart.js` — _(điền lý do)_

## 3. Key Decisions

- Cost-cap hoàn NCC: làm **server-authoritative** (`loadSoOrderCostByCodeMap`) thay vì bắt frontend gửi `rowReturns`.
    - Lý do: client không sửa được cost; frontend aggregate đã bỏ row IDs nên dựng rowReturns khó + vẫn trust client.
    - Alternative reject: (a) frontend gửi rowReturns (phức tạp, vẫn client-trust); (b) lưu cost lên web2_products (không có cột cost, cost chỉ ở so-order).
- Cap **fail-open** (chỉ cap khi MỌI line tra được cost) để không block refund hợp lệ khi so-order wipe / SP chưa match.
- Auth native-orders: gate 11 route frontend-only bằng `requireWeb2AuthSoft` (parity fast-sale-orders); **CHỪA `/from-comment`** vì cart.js loopback gọi không kèm token.

## 4. Open Todos / Next Steps

- [ ] **`/from-comment` auth**: forward `x-web2-token` trong cart.js loopback (`v2/cart.js:232`) rồi mới gate route (hiện để open, chỉ tạo draft).
- [ ] **returned_row_ids tracking cho quick-refund** (audit #3 HIGH): không gửi rowReturns → không track SL đã trả per so-order row → over-refund qty xuyên nhiều phiếu không bị chặn. Cần frontend gửi rowReturns hoặc backend infer.
- [ ] **wallet `:phone` validation** (MEDIUM): deposit/withdraw chưa validate — cẩn thận strict `/^0\d{9}$/` loại ví 9–11 số đang tồn tại.
- [ ] **Frontend cleanup**: comment "quick-refund KHÔNG notify web2:products" trong purchase-refund-actions.js giờ stale (server đã notify); refresh-cache tay còn lại vô hại.
- [ ] **Verify live sau Render redeploy**: quick-refund cap về cost; cart 2-tab không lost-update; tab khác thấy tồn kho/ví NCC update không refresh.

## 5. Context Pointers

<!-- File / doc / URL cần đọc để vào việc tiếp. Đặt đường dẫn để Claude Read trực tiếp.
- `docs/...`
- `path/to/code.js:LINE_RANGE`
- External: <URL>
-->

## 6. Notes for Next Session

<!-- Cảnh báo, gotcha, dependency cần check, env state, browser session đang chạy, etc. -->

## 7. Previous Session (Chain Pointer)

> Một conversation có thể có nhiều token tạo dần qua các turn. Section này link đến session trước để Claude tự lần ngược, ghép đủ ngữ cảnh khi user chỉ paste **token cuối**.

- Previous: [`RESUME:20260623-183215-80cfd2d`](./20260623-183215-80cfd2d.md)

**Chain walking** (Claude tự làm khi nhận token):

1. Đọc file hiện tại.
2. Nếu Previous ≠ "INITIAL" → đọc thêm file previous.
3. Lặp lại tối đa **N=3 levels** mặc định (tránh context bloat).
4. Nếu user nói "đọc full chain" → đi hết tới INITIAL.

---

## How to resume

User paste vào chat mới:

```
RESUME:20260623-184556-af2ca38
```

Claude sẽ:

1. `Read` file này.
2. Walk chain theo section 7 (mặc định 3 levels, hoặc theo yêu cầu user).
3. Tóm tắt 2-3 câu để xác nhận hiểu đúng.
4. Tiếp tục từ phần "Next Steps" của session gần nhất.
