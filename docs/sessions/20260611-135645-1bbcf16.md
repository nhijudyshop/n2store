<!-- #Note: Đọc CLAUDE.md, MEMORY.md, docs/dev-log.md trước khi code. Cập nhật dev-log sau thay đổi. | Read these files before coding, update dev-log after changes. -->

# Session Resume — 2026-06-11 13:56:45 +07

**Token**: `RESUME:20260611-135645-1bbcf16`
**Branch**: `main`
**HEAD**: `1bbcf16e9`
**Author**: zxGreenxz <nguyentamca165@gmail.com>
**Generated**: 2026-06-11T06:56:45Z
**Note**: file tạo THỦ CÔNG (máy Windows không có python cho save-session-resume.sh).

---

## 1. Summary

feat(showroom1): trang chi tiết sản phẩm — bấm vào ảnh trên card → sheet gần full màn hình: ảnh LỚN swipe qua lại + dấu chấm đếm số ảnh (bấm dot nhảy ảnh), dưới ảnh là thông tin SP: tên, giá, **Size (không có size → chip "Freesize")**, Màu, **khối mô tả sản phẩm** (size theo số ký, chất liệu… do shop nhập), nút "Thêm vào giỏ" (đóng detail → picker/fly). Admin desktop có thêm **textarea "Mô tả sản phẩm"** trong drawer sửa SP, lưu vào cột mới `showroom_products.description` (backend commit `94aff7799`). Đã verify Playwright localhost + smoke prod.

## 2. Files Modified

- `render.com/routes/showroom-products.js` — cột `description TEXT` (ALTER idempotent), `sanitizeText` giữ newline cắt 2000, POST/PUT/COLS/rowToProduct (commit `94aff7799`, deployed)
- `showroom1/detail.js` (NEW) — `window.ShowroomDetail.open(product, startIdx)`: carousel swipe + dots, info, Freesize, mô tả multi-line, add-to-cart
- `showroom1/detail.css` (NEW) — z-index 84 (dưới cart 85/picker 86), ảnh aspect 3:4, dots active giãn dài, khối desc nền kem
- `showroom1/index.html` — tap ảnh (không vuốt, có dataset.id) → mở detail đúng tấm đang xem (`w.__idx`); load detail.css/js; bump `?v=20260611d`
- `showroom1/admin.js` — textarea `#fDesc` + openEditor/saveDraft + toPreview `description`
- `showroom1/admin.css` — style textarea
- `docs/dev-log.md` — entry mới (commit `1bbcf16e9`)

## 3. Key Decisions

- Detail = sheet thứ 3 tái dùng CSS `.sheet` (z 84) — "Thêm vào giỏ" từ detail **đóng detail trước** rồi gọi `addWithOptions` để picker/pill/fly hiện rõ (không chồng sheet che pill).
- Mô tả size theo số ký = **free-text 1 field `description`** (placeholder gợi ý "S: dưới 48kg · M: 49-55kg…") thay vì mapping per-size — đơn giản cho shop, đủ linh hoạt.
- "Freesize" chỉ là HIỂN THỊ mặc định khi `sizes` rỗng (detail viewer) — không lưu vào DB, không ảnh hưởng picker (SP không size vẫn thêm thẳng).

## 4. Open Todos / Next Steps

- [ ] Shop vào admin desktop điền "Mô tả sản phẩm" cho từng SP (mô tả test đã được reset về trống — tránh thông tin bịa trên SP thật).
- [ ] **Điền CONTACT thật** đầu `showroom1/cart.js` (phone/zalo placeholder).
- [ ] **TRUNCATE showroom_carts RESTART IDENTITY** trên Render DB trước khi gửi link cho khách thật (counter test đã tới #21; giỏ #6 còn item test kẹt token).
- [ ] (Hardening) token query-string → header.

## 5. Context Pointers

- `showroom1/detail.js` — toàn bộ viewer; `showroom1/index.html` click handler trong `bindImgwrap`
- `docs/dev-log.md` — 4 entry showroom1 ngày 2026-06-11 (v1 visitor ID, v2 UX nút giỏ/picker, v3 fly animation, v4 detail)

## 6. Notes for Next Session

- ⚠ Session song song trên cùng working tree: **localhost:8080 bị app CRM (Vite/React) của họ chiếm** → browser test showroom1 dùng port khác (đã dùng 8099). Stage đích danh file khi commit.
- WIP live-chat của session kia có thể vẫn ở `stash@{0}`.

## 7. Previous Session (Chain Pointer)

- Previous: [`RESUME:20260611-133202-b4494df`](./20260611-133202-b4494df.md) _(cùng mạch showroom1 — fly animation)_

**Chain walking** (Claude tự làm khi nhận token):

1. Đọc file hiện tại.
2. Nếu Previous ≠ "INITIAL" → đọc thêm file previous.
3. Lặp lại tối đa **N=3 levels** mặc định (tránh context bloat).
4. Nếu user nói "đọc full chain" → đi hết tới INITIAL.

---

## How to resume

User paste vào chat mới:

```
RESUME:20260611-135645-1bbcf16
```

Claude sẽ:

1. `Read` file này.
2. Walk chain theo section 7 (mặc định 3 levels, hoặc theo yêu cầu user).
3. Tóm tắt 2-3 câu để xác nhận hiểu đúng.
4. Tiếp tục từ phần "Next Steps" của session gần nhất.
