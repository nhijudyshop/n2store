<!-- #Note: Đọc CLAUDE.md, MEMORY.md, docs/dev-log.md trước khi code. Cập nhật dev-log sau thay đổi. | Read these files before coding, update dev-log after changes. -->

# Session Resume — 2026-06-05 15:18:08 SEAST

**Token**: `RESUME:20260605-151808-aeb78f0`
**Branch**: `main`
**HEAD**: `aeb78f00a50e0b760c9c706c0a05924f6ad6f3e1`
**Author**: zxGreenxz <nguyentamca165@gmail.com>
**Generated**: 2026-06-05T08:18:08Z

---

## 1. Summary

fix: đánh KPI base sau gửi tin nhắn hàng loạt chỉ ~nửa 600 đơn được đánh — base rớt vì lấy SP fallback TPOS tuần tự nuốt lỗi im + chỉ xét đơn gửi OK.

## 2. Files Modified (commit `aeb78f0`)

- `orders-report/js/managers/kpi-manager.js` — `saveAutoBaseSnapshot` viết lại (tier-3 song song+retry, lưu theo lô+verify, return giàu hơn); `fetchProductsFromTPOS` throw on HTTP error.
- `orders-report/js/chat/message-template-manager.js` — `_resolveOrderData` cache writeback; `_saveCampaignResults` base cho MỌI đơn (success+error) + đính Details từ cache + campaignName robust + toast kết quả.
- `orders-report/js/tab1/tab1-kpi-base-button.js` — message dùng field `noProduct`/`failed`.
- `docs/dev-log.md` — entry.

## 3. Key Decisions

- **Root cause**: AUTO path `_saveCampaignResults` → `saveAutoBaseSnapshot(successOrders,...)`. (1) chỉ đơn GỬI OK; (2) SP 3 tầng: Tier1 report map cần `campaignName` (`window.currentCampaignName` là `let` overview-core.js → undefined), Tier2 `order.Details` rỗng (success result không kèm SP), Tier3 `fetchProductsFromTPOS` **tuần tự ~600 lần, nuốt lỗi im** → rate-limit giữa chừng → `[]` → `continue` bỏ qua âm thầm; (3) batch 1 request không verify; (4) nuốt lặng.
- **Owner chốt**: base cho **MỌI đơn đã chọn (kể cả gửi lỗi)** → tách base khỏi kết quả gửi (gom `[...successOrders, ...errorOrders]`). + muốn thấy kết quả rõ (chưa chắc lỗi khâu nào).
- **Fix client-side, KHÔNG sửa endpoint KPI/TPOS** (memory `feedback_api_scope`): chỉ gọi lại `/kpi-base/check-exists`, `/kpi-base/batch`, TPOS OData. Tier-3 song song concurrency 8 + retry/đơn; phân biệt lỗi-fetch (`failed`) vs rỗng-thật (`noProduct`) không đếm trùng; lưu lô 100 + verify + retry lô.
- Reuse `_orderDetailsCache` (message-template-manager) + `_resolveOrderData` (fix task trước) → KPI tái dùng SP, khỏi refetch.

## 4. Open Todos / Next Steps

- [ ] **Owner verify LIVE**: gửi 1 lô đơn nhiều SP → xem toast `KPI base: đã đánh X/Y (Z thiếu SP, W lỗi)` + console `[KPI]`. Tab "KPI - HOA HỒNG" thấy đủ. (Chưa verify live được trên Windows box — thiếu Python + script Playwright target macOS.)
- [ ] Nếu vẫn thiếu: đọc console `[KPI] saveAutoBaseSnapshot: saved=.. noProduct=.. failed=..` để biết khâu nào.

## 5. Context Pointers

- `orders-report/js/managers/kpi-manager.js`: `fetchProductsFromTPOS` (~215), `saveAutoBaseSnapshot` (~243).
- `orders-report/js/chat/message-template-manager.js`: `_resolveOrderData` (~805, cache writeback), `_saveCampaignResults` (~1849, base cho mọi đơn + toast).
- `orders-report/js/tab1/tab1-kpi-base-button.js` (~60).
- Plan: `C:\Users\Nguyen Tam\.claude\plans\a-i-sao-n-memoized-hearth.md`

## 6. Notes for Next Session

- **Máy Windows, KHÔNG Python** → `save-session-resume.sh` không chạy (file này tạo tay; folder snapshots `docs/sessions/latest/*` KHÔNG cập nhật lần này).
- **⚠ Repo đang bị auto-sync mạnh**: có process khác (máy/phiên khác) liên tục `git pull --rebase` reset working tree → **commit ngay sau khi sửa** kẻo mất uncommitted. Session này từng bị reset 1 lần, phải re-apply toàn bộ edit.
- Liên quan task trước (cùng file message-template-manager): fix `(Chưa có sản phẩm)` đơn nhiều SP — `RESUME:20260605-095847-7063a1e`.

## 7. Previous Session (Chain Pointer)

> Một conversation có thể có nhiều token tạo dần qua các turn. Section này link đến session trước.

- Previous: [`RESUME:20260605-151446-a76f415`](./20260605-151446-a76f415.md)

**Chain walking** (Claude tự làm khi nhận token):

1. Đọc file hiện tại.
2. Nếu Previous ≠ "INITIAL" → đọc thêm file previous.
3. Lặp tối đa **3 levels** mặc định.
4. Nếu user nói "đọc full chain" → đi hết tới INITIAL.

---

## How to resume

User paste vào chat mới:

```
RESUME:20260605-151808-aeb78f0
```
