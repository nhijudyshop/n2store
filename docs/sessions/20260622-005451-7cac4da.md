<!-- #Note: Đọc CLAUDE.md, MEMORY.md, docs/dev-log.md trước khi code. Cập nhật dev-log sau thay đổi. | Read these files before coding, update dev-log after changes. -->

# Session Resume — 2026-06-22 00:54:51 +07

**Token**: `RESUME:20260622-005451-7cac4da`
**Branch**: `main`
**HEAD**: `7cac4dab810d0cf4cce9e763f0a86cfae5ea8775`
**Range**: `c538ee8..7cac4da` (3 commit(s))
**Author**: nhijudyshop <147459165+nhijudyshop@users.noreply.github.com>
**Generated**: 2026-06-21T17:54:51Z

---

## 1. Summary

fix(web2) SSE realtime backbone: cross-instance fan-out Postgres LISTEN/NOTIFY + observability + graceful deploy + 3 review rounds (19→6→4 finding hội tụ)

<!-- 2-4 câu mở rộng: phiên này đã làm gì, vì sao, kết quả ra sao -->

## 2. Files Modified

<!-- Liệt kê file + ý nghĩa thay đổi, không paste diff. Format:
- `path/to/file.js` — short why
-->

- `docs/dev-log.md` — _(điền lý do)_
- `docs/sessions/20260622-004507-c538ee8.md` — _(điền lý do)_
- `docs/sessions/latest/_all.md` — _(điền lý do)_
- `docs/sessions/latest/render.com.md` — _(điền lý do)_
- `render.com/routes/realtime-sse-web2.js` — _(điền lý do)_

## 3. Key Decisions

- **Cross-instance SSE = Postgres LISTEN/NOTIFY** (web2Db channel `web2_sse`), KHÔNG Redis. Lý do: web2Db sẵn có, hub vốn single-process; pub/sub qua DB đủ cho quy mô shop.
- **CHỈ web2-api init fan-out** (gate `!WEB2_API_FORWARD_URL`); fallback chỉ HTTP-relay. Lý do: 2 service chung 1 web2Db → nếu cả 2 pg-NOTIFY thì double-deliver.
- **BOOT_ID luôn random suffix** (KHÔNG slice RENDER_INSTANCE_ID) — slice cắt nhầm phần per-instance → trùng → fan-out vỡ.
- **Wildcard/broadcast PHẢI forward `kind` qua relay** — nếu không SePay ví không refresh (regression vòng 2).
- **3 vòng adversarial review** (19→6→4) thay vì 1 — vì fix vòng 1 tự sinh regression vòng 2 (gate giết wildcard). Review-the-fix là cần thiết.
- Đã CHỐT hội tụ sau vòng 3 (chỉ còn MED/LOW edge-case bounded bởi Render 30s SIGKILL).

## 4. Open Todos / Next Steps

- [ ] (Theo dõi) Quan sát `/sse/stats` `multiInstanceWarning`/`deliveredFromPeers` trong cửa sổ deploy thật để xác nhận fan-out giao cross-instance (deliveredFromPeers>0) khi mutation+SSE lệch instance.
- [ ] (Tùy) Bump `web2-sidebar.js?v=` toàn trang để prod hiện menu TV Livestream/Điều khiển TV (sweep ~47 file) — trang vẫn vào được bằng URL.
- [ ] (LOW, defer) Finding r3 #3 pg_notify share LISTEN client: reviewer chốt acceptable; nếu /ingest burst tăng mạnh → tách dedicated NOTIFY connection.

## 5. Context Pointers

- MEMORY: `reference_web2_sse_cross_instance.md`, `reference_web2_live_tv.md`
- `render.com/routes/realtime-sse-web2.js` (hub: notifyClients/\_localNotify, \_publishCrossInstance/\_onCrossInstance, \_connectListen/\_heartbeat/initCrossInstance, gracefulClose, /sse/stats)
- `render.com/server.js` (initCrossInstance gate ~753, gracefulShutdown ~3061)
- `render.com/routes/web2-campaign-products.js` (PATCH /pending dual-broadcast)
- Verify: browser-session `/sse/stats` (bootId/liveInstances/crossStats) + Web2SSE.subscribe delivery test.

## 6. Notes for Next Session

<!-- Cảnh báo, gotcha, dependency cần check, env state, browser session đang chạy, etc. -->

## 7. Previous Session (Chain Pointer)

> Một conversation có thể có nhiều token tạo dần qua các turn. Section này link đến session trước để Claude tự lần ngược, ghép đủ ngữ cảnh khi user chỉ paste **token cuối**.

- Previous: [`RESUME:20260622-004507-c538ee8`](./20260622-004507-c538ee8.md)

**Chain walking** (Claude tự làm khi nhận token):

1. Đọc file hiện tại.
2. Nếu Previous ≠ "INITIAL" → đọc thêm file previous.
3. Lặp lại tối đa **N=3 levels** mặc định (tránh context bloat).
4. Nếu user nói "đọc full chain" → đi hết tới INITIAL.

---

## How to resume

User paste vào chat mới:

```
RESUME:20260622-005451-7cac4da
```

Claude sẽ:

1. `Read` file này.
2. Walk chain theo section 7 (mặc định 3 levels, hoặc theo yêu cầu user).
3. Tóm tắt 2-3 câu để xác nhận hiểu đúng.
4. Tiếp tục từ phần "Next Steps" của session gần nhất.
