<!-- #Note: Đọc CLAUDE.md, MEMORY.md, docs/dev-log.md trước khi code. Cập nhật dev-log sau thay đổi. | Read these files before coding, update dev-log after changes. -->

# Session Resume — 2026-06-16 18:21:22 +07

**Token**: `RESUME:20260616-182122-306e6ce`
**Branch**: `main`
**HEAD**: `306e6ce6c0d9df9747a660811c99422b7befcc35`
**Range**: `15c4e7a..306e6ce` (1 commit, sau rebase)
**Generated**: 2026-06-16T11:21:22Z

> ⚠ File này tạo TAY (Windows machine không có python3 → `scripts/save-session-resume.sh` không chạy được; snapshot `docs/sessions/latest/*.md` KHÔNG được cập nhật phiên này).

---

## 1. Summary

feat(customer-hub): double-click cột "Ví khách hàng" → xếp khách có công nợ (số dư ví ≠ 0) lên đầu.

User muốn nhấp đúp header cột ví để bubble khách có công nợ lên trên. Vì danh sách KH là infinite-scroll (20/trang) + ví chỉ enrich theo từng trang đã load → sort client-side chỉ sắp được ~20 dòng. Phát hiện route `/v2/customers/recent` **vốn đã LEFT JOIN customer_wallets** → thêm sort server-side `sort=wallet` (ABS(balance+virtual) DESC) cho sort TOÀN CỤC rẻ + đúng. Đã chạy adversarial review workflow → fix 3 lỗi (tie-break c.id, wallet-updated re-sort, prependRow khi sort bật). Commit + push xong.

## 2. Files Modified

- `render.com/routes/v2/customers.js` — `/recent` thêm param optional `sort=wallet` (whitelist, ORDER BY ABS(balance) DESC + tie-break `c.id ASC`). **CẦN DEPLOY RENDER.**
- `shared/js/api-service.js` — `getRecentCustomers(page, limit, sort='')` backward-compat.
- `customer-hub/js/modules/customer-search.js` — `sortMode`, dblclick header, `_toggleWalletSort/_sortByWallet/_updateWalletSortIndicator`, guard wallet-updated + 2 path tạo KH.
- `docs/dev-log.md` — entry mới.

## 3. Key Decisions

- **Server-side sort (không client-side)**: vì pagination + ví enrich per-page → client chỉ sort được dòng đã load. Backend đã có JOIN ví sẵn nên thêm `ORDER BY` là đủ, sort toàn cục 1 query.
  - Alternative reject: loop loadMore tải hết rồi sort client (N request, nặng nếu nhiều KH).
- **ABS(số dư) DESC** (không signed): "có công nợ" = số dư ≠ 0 bất kể dấu → gom cả dư có (+) lẫn dư nợ (−) lên đầu, số 0 chìm. Nếu user muốn CHỈ nợ (âm) lên đầu → đổi sang signed ASC.
- **Param optional + whitelist** trên endpoint Web 1.0 prod `/recent`: mặc định byte-identical, chỉ customer-hub consume (render-data-manager chỉ liệt kê path) → blast radius nhỏ.
- **Tie-break `c.id ASC`**: `c.phone` KHÔNG unique trên bảng `customers` (xem route /duplicates) + khối ABS=0 lớn → thiếu tie-break tổng quát gây trùng/sót dòng khi phân trang.
- **Inline style** cho `user-select`/`opacity`: 3 class Tailwind (`select-none`, `opacity-40/100`) chưa có trong `customer-hub/styles/tailwind.css` đã build (served trên GH Pages) → tránh rebuild CSS toolchain.

## 4. Open Todos / Next Steps

- [ ] **DEPLOY Render** (web 1.0 api) để `sort=wallet` có hiệu lực — frontend đã live trên GH Pages sau push nhưng backend chưa.
- [ ] Verify end-to-end trên prod sau deploy: nhấp đúp cột Ví → khách có số dư lên đầu; cuộn qua nhiều trang không trùng/sót dòng.
- [ ] (Optional) Nếu user muốn "công nợ" = chỉ dư nợ (âm) → đổi ABS DESC sang signed ASC ở cả backend `/recent` và frontend `_sortByWallet`.

## 5. Context Pointers

- `render.com/routes/v2/customers.js` — route `/recent` (~line 257-300).
- `customer-hub/js/modules/customer-search.js` — `_toggleWalletSort` + `renderResults` + helper sort (~line 754-810).
- `shared/js/api-service.js:1628` — `getRecentCustomers`.
- URL: https://nhijudy.store/customer-hub/index.html

## 6. Notes for Next Session

- Backend Render KHÔNG auto-effect sau git push — cần deploy thủ công/CI Render. Frontend GH Pages thì auto sau ~2-4 phút.
- `batch-summary.total = balance + virtual_balance` ≡ biểu thức JOIN của `/recent` → số hiển thị khớp thứ tự sort.
- Windows machine: không python3 → script save-session + nhiều script test bash không chạy; snapshot `docs/sessions/latest/*` phiên này KHÔNG cập nhật.

## 7. Previous Session (Chain Pointer)

- Previous: [`RESUME:20260616-181427-560d407`](./20260616-181427-560d407.md)

**Chain walking** (Claude tự làm khi nhận token):

1. Đọc file hiện tại.
2. Nếu Previous ≠ "INITIAL" → đọc thêm file previous.
3. Lặp lại tối đa **N=3 levels** mặc định (tránh context bloat).
4. Nếu user nói "đọc full chain" → đi hết tới INITIAL.

---

## How to resume

User paste vào chat mới:

```
RESUME:20260616-182122-306e6ce
```

Claude sẽ:

1. `Read` file này.
2. Walk chain theo section 7 (mặc định 3 levels, hoặc theo yêu cầu user).
3. Tóm tắt 2-3 câu để xác nhận hiểu đúng.
4. Tiếp tục từ phần "Next Steps" của session gần nhất.
