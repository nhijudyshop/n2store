<!-- #Note: Đọc CLAUDE.md, MEMORY.md, docs/dev-log.md trước khi code. Cập nhật dev-log sau thay đổi. | Read these files before coding, update dev-log after changes. -->

# Session Resume — 2026-06-11 11:30:44 +07

**Token**: `RESUME:20260611-113044-ecc0c4c`
**Branch**: `main`
**HEAD**: `ecc0c4c77`
**Author**: zxGreenxz <nguyentamca165@gmail.com>
**Generated**: 2026-06-11T04:30:44Z
**Note**: file tạo THỦ CÔNG (máy Windows không có python cho save-session-resume.sh).

---

## 1. Summary

feat(showroom1) UX giỏ hàng v2 theo feedback user (cùng conversation với RESUME:20260611-103147-4805aee): (1) pill mã khách nâng lên bottom 54px khỏi mép bo phone, xóa cookie FAB thừa bên trái; (2) icon trái tim trên card → nút giỏ hàng tròn trắng (`.addbag`, lucide shopping-cart) — khách bấm NÚT để thêm, **bấm ảnh không còn thêm giỏ** (ảnh chỉ swipe carousel); (3) bấm thêm → **sheet chọn size/màu** đơn giản chữ to nút to cho người ~40 tuổi (chips 48px, nhóm 1 lựa chọn tự chọn sẵn, thiếu chọn → toast giữ sheet). Item giỏ giờ có size/color — cùng SP khác size/màu = dòng riêng; hiển thị variant ở cart sheet khách + admin "Giỏ hàng khách". Đã verify Playwright localhost + smoke prod (visitor #9, "ĐẦM TAY DÀI HT (L · XANH)").

## 2. Files Modified

- `render.com/routes/showroom-carts.js` — sanitizeItems nhận `size`/`color` (≤40, strip `<>`), dedupe theo productId+size+color (commit `2de07b4b6`, đã deploy + verify prod)
- `showroom1/index.html` — floaties 54px, xóa cookie FAB + `.fab.chat` chết, `.fav`→`.addbag`, bỏ click-ảnh-thêm, `bindAddBag`→`ShowroomCart.addWithOptions`, map `window.Showroom._products`, ?v=20260611b
- `showroom1/cart.js` — `addWithOptions` + picker sheet `#pickSheet` (z-index 86), item size/color, row giỏ theo index, alias `add`=addWithOptions
- `showroom1/cart.css` — `.pick-*` chips to, `.cart-variant`
- `showroom1/admin.js` / `admin.css` — item giỏ khách hiện `M · CAM` (esc(), màu accent)
- `docs/dev-log.md` — entry "[showroom1] UX giỏ hàng v2"

## 3. Key Decisions

- **Bỏ hẳn tap-ảnh-thêm-giỏ** (yêu cầu user) — thêm giỏ CHỈ qua nút `.addbag`; ảnh giữ swipe carousel. Wishlist (trái tim) bỏ hoàn toàn.
- **Picker không preselect** khi nhóm có nhiều lựa chọn (tránh đặt nhầm size mặc định), CHỈ tự chọn khi nhóm có đúng 1 lựa chọn; validate bằng toast thân thiện thay vì disable nút.
- **Dòng giỏ = SP × size × màu** (composite key cả client lẫn server) — row trong sheet tham chiếu theo index mảng (tránh escape key phức tạp trong dataset).
- Full product (sizes/colors/images) truyền cho cart.js qua map `window.Showroom._products` do `renderGrid` build — không nhét JSON vào dataset.

## 4. Open Todos / Next Steps

- [ ] **Điền CONTACT thật** đầu `showroom1/cart.js` (phone + zalo placeholder `0900000000`; messenger đoán `m.me/NhiJudyStore`) → bump `cart.js?v=`.
- [ ] **Reset counter về #1 trước khi gửi link cho khách thật**: visitor #1–#9 đã dùng test (giỏ đã empty, riêng **#6 còn 2 item test không có token để xóa**) → chạy `TRUNCATE showroom_carts RESTART IDENTITY;` trên Render DB (n2store chatDb) — sạch test data + khách thật bắt đầu #1.
- [ ] (Hardening sau) token qua query-string GET → chuyển header.

## 5. Context Pointers

- `docs/dev-log.md` — 2 entry showroom1 ngày 2026-06-11 (v1 + v2)
- `showroom1/cart.js` — picker: `openPicker`/`renderPickerChips`/`buildPicker`; cart sheet: `renderSheet`
- `showroom1/index.html` — `bindAddBag`, `bindImgwrap` (chỉ swipe), `renderGrid` (map _products)
- Session trước cùng mạch: [`./20260611-103147-4805aee.md`](./20260611-103147-4805aee.md)

## 6. Notes for Next Session

- ⚠ Vẫn có session Claude song song trên working tree này (live-chat/wallet): khi commit phải stage đích danh + commit ngay; **WIP live-chat của họ đang nằm ở `stash@{0}` (autostash)** sau sự cố pull --rebase --autostash lúc 11:0x — họ cần `git stash pop` + resolve conflict với commit `354e8a1fc` (fix GMT+7) khi quay lại.
- Máy Windows này KHÔNG có python → `save-session-resume.sh` fail; tạo session file thủ công.

## 7. Previous Session (Chain Pointer)

> Một conversation có thể có nhiều token tạo dần qua các turn. Section này link đến session trước để Claude tự lần ngược, ghép đủ ngữ cảnh khi user chỉ paste **token cuối**.

- Previous: [`RESUME:20260611-103147-4805aee`](./20260611-103147-4805aee.md) _(cùng mạch showroom1 — visitor ID + giỏ hàng v1)_

**Chain walking** (Claude tự làm khi nhận token):

1. Đọc file hiện tại.
2. Nếu Previous ≠ "INITIAL" → đọc thêm file previous.
3. Lặp lại tối đa **N=3 levels** mặc định (tránh context bloat).
4. Nếu user nói "đọc full chain" → đi hết tới INITIAL.

---

## How to resume

User paste vào chat mới:

```
RESUME:20260611-113044-ecc0c4c
```

Claude sẽ:

1. `Read` file này.
2. Walk chain theo section 7 (mặc định 3 levels, hoặc theo yêu cầu user).
3. Tóm tắt 2-3 câu để xác nhận hiểu đúng.
4. Tiếp tục từ phần "Next Steps" của session gần nhất.
