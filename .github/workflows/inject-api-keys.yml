name: Inject API Keys

on:
  push:
    branches:
      - main
      - claude/**
    paths:
      - 'invoice-compare/**'
  workflow_dispatch:

jobs:
  inject-keys:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Inject API Keys into HTML
        run: |
          # Inject GEMINI_PRO_KEY (primary key)
          sed -i "s|window.GEMINI_PRO_KEY = savedGeminiProKey \|\| '' \|\| '';|window.GEMINI_PRO_KEY = savedGeminiProKey \|\| '${{ secrets.GEMINI_PRO_KEY }}' \|\| '';|g" invoice-compare/index.html
          
          echo "✅ GEMINI_PRO_KEY injected successfully"

      - name: Verify injection
        run: |
          echo "Checking if PRO key was injected..."
          grep -q "GEMINI_PRO_KEY = savedGeminiProKey" invoice-compare/index.html && echo "✅ GEMINI_PRO_KEY pattern found" || echo "❌ Pattern not found"

      - name: Commit changes (if any)
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

          if git diff --quiet invoice-compare/index.html; then
            echo "No changes to commit"
          else
            git add invoice-compare/index.html
            git commit -m "🔑 Auto-inject API keys from secrets [skip ci]"
            git push
            echo "✅ Changes committed and pushed"
          fi

      - name: Display success message
        run: |
          echo "=================================================="
          echo "✅ GEMINI PRO KEY Injection Complete"
          echo "=================================================="
          echo ""
          echo "PRO key has been injected into:"
          echo "  - invoice-compare/index.html"
          echo ""
          echo "The application will now use:"
          echo "  1. localStorage (if user set manually)"
          echo "  2. GitHub Secrets GEMINI_PRO_KEY (injected by this workflow)"
          echo ""
          echo "=================================================="
